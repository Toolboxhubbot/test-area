-- Auto-load Anti-Fling (safe loader — won't crash if URL is down)
task.spawn(function()
    local url = "https://raw.githubusercontent.com/Toolboxhubbot/antifling/refs/heads/main/antifling"
    local src = nil
    pcall(function()
        if game.HttpGet then src = game:HttpGet(url) end
    end)
    if not src and syn and syn.request then
        pcall(function()
            local r = syn.request({Url = url, Method = "GET"})
            if r and r.Body then src = r.Body end
        end)
    end
    if not src and request then
        pcall(function()
            local r = request({Url = url, Method = "GET"})
            if r and r.Body then src = r.Body end
        end)
    end
    if not src or #src < 10 then
        warn("[Appleware] Anti-fling fetch failed - script continues normally")
        return
    end
    local fn
    pcall(function() fn = loadstring(src) end)
    if fn then
        pcall(function() fn() end)
    else
        warn("[Appleware] Anti-fling script didn't compile - skipping")
    end
end)

LocalTeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")
local TextChatService = game:GetService("TextChatService")
local LocalPlayer = Players.LocalPlayer
local PlaceId = game.PlaceId

local function serverHop()
    pcall(function()
        if QueueTeleport then QueueTeleport("https://www.roblox.com/games/" .. PlaceId) end
    end)
    pcall(function() LocalTeleportService:Teleport(PlaceId, LocalPlayer) end)
end

local function handleChatCommand(message)
    local msg = message:lower()
    if msg == "!shop" or msg:sub(1, 6) == "!shop " then serverHop() end
end

LocalPlayer.Chatted:Connect(handleChatCommand)

pcall(function()
    if TextChatService.ChatInputBarConfiguration and TextChatService.ChatInputBarConfiguration.TargetTextChannel then
        TextChatService.MessageReceived:Connect(function(textChatMessage)
            if textChatMessage.TextSource and textChatMessage.TextSource.UserId == LocalPlayer.UserId then
                handleChatCommand(textChatMessage.Text)
            end
        end)
    end
end)

-- ====================================================================
-- Appleware
-- ====================================================================
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")
local TeleportService = game:GetService("TeleportService")
local Lighting = game:GetService("Lighting")
local UserGameSettings = UserSettings():GetService("UserGameSettings")
local GuiService = game:GetService("GuiService")
local UIS = game:GetService("UserInputService")
local StatsService = game:GetService("Stats")

local LP = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

pcall(function()
    if setclipboard then setclipboard("https://discord.gg/ux2fjm2rt8") end
end)

local function getGuiParent()
    local success, gui = pcall(function()
        if gethui then return gethui()
        elseif syn and syn.protect_gui then
            local c = Instance.new("Folder")
            c.Name = "ProtectedGuiContainer"
            syn.protect_gui(c)
            c.Parent = CoreGui
            return c
        elseif CoreGui:FindFirstChild("RobloxGui") then return CoreGui end
        return LP:WaitForChild("PlayerGui", 5)
    end)
    if success and gui then return gui end
    return LP:FindFirstChild("PlayerGui") or CoreGui
end

local CONFIG_FOLDER_NAME = "ApplewareConfigs"
local CONFIG_FILE_NAME = "Appleware.json"

local function ensureConfigFolder()
    pcall(function()
        if makefolder and isfolder and not isfolder(CONFIG_FOLDER_NAME) then makefolder(CONFIG_FOLDER_NAME) end
    end)
end
ensureConfigFolder()

local function getFullPath()
    if isfolder and isfolder(CONFIG_FOLDER_NAME) then return CONFIG_FOLDER_NAME .. "/" .. CONFIG_FILE_NAME end
    return CONFIG_FILE_NAME
end

local saveSettings, buildUI, st

local state = {
    farm = false, xpFarm = false, noclip = false, gun = false, afk = false,
    antiVoid = false, notifyMapName = false, showRoundTimerCountdown = false,
    autoKillAll = false, autoShootMur = false, autoFlingMur = false,
    autoResetMurderer = false, autoResetSheriff = false, autoResetInnocent = false,
    aimbotMurderer = false, aimbotSheriff = false,
    loopFlingAll = false, loopFlingMur = false, loopFlingSheriff = false, loopFlingCustom = false,
    disable3d = false, fpsBoost = false, sendOnFull = false, autoRejoin = false,
    autoServerHop = false, autoPrestige = false, espPlayers = false, espGunDrop = false,
    quickTogglesVisible = true,
    flyEnabled = false, flySpeed = 50,
    speedEnabled = false, speedValue = 16,
    jumpEnabled = false, jumpValue = 50,
    infJumpEnabled = false,
    invisibleEnabled = false,
    instantInteract = false,
    cameraZoomEnabled = false,
    cameraMaxZoom = 500,
    infoHudEnabled = false,
    antiRagdoll = true,
    freeze = false,
    fullbright = false,
    fovEnabled = false, fovValue = 70,
    clickTP = false,
}

local settingsConfig = {
    webhookUrl = "", webhookCooldown = 30, discordUserId = "",
    tweenSpeed = 25, selectedScale = 1, roundLength = 200,
    flingList = {}, whitelist = {},
    quickTogglesList = {
        "Teleport to Map", "Teleport to Murderer", "Teleport to Sheriff",
        "Grab Gun Now", "Kill All Now", "Kill Sheriff Now", "Kill Custom Now",
        "Fling All Now", "Fling Murderer Now", "Fling Sheriff Now", "Fling Custom Now",
        "Aimbot Murderer", "Aimbot Sheriff",
    }
}

local TWEEN_SPEED = settingsConfig.tweenSpeed or 25
local ROUND_LENGTH_FALLBACK = settingsConfig.roundLength or 200
local LOBBY_CFRAME = CFrame.new(14.20, 516.73, -25.15)
local RESET_DURATION = 3

local toggleDisplayNames = {
    farm = "Farm (Safe)", xpFarm = "XP Farm", noclip = "Noclip", gun = "Auto Grab Gun",
    afk = "Anti AFK", antiVoid = "Anti-Void", notifyMapName = "Notify Map Name",
    showRoundTimerCountdown = "Round Timer", autoKillAll = "Auto Kill All",
    autoShootMur = "Auto Shoot Murderer", autoFlingMur = "Auto Fling Murderer",
    autoResetMurderer = "Auto Reset: Murderer", autoResetSheriff = "Auto Reset: Sheriff",
    autoResetInnocent = "Auto Reset: Innocent", aimbotMurderer = "Aimbot: Murderer",
    aimbotSheriff = "Aimbot: Sheriff", loopFlingAll = "Loop Fling All",
    loopFlingMur = "Loop Fling Murderer", loopFlingSheriff = "Loop Fling Sheriff",
    loopFlingCustom = "Loop Fling Custom", disable3d = "Disable 3D Rendering",
    fpsBoost = "FPS Boost (Potato)", sendOnFull = "Auto Webhook", autoRejoin = "Auto Rejoin",
    autoServerHop = "Auto Serverhop", autoPrestige = "Auto Prestige",
    espPlayers = "Player ESP (Roles)", espGunDrop = "Gun Drop ESP",
    quickTogglesVisible = "Quick Toggles Menu",
    flyEnabled = "Fly", infJumpEnabled = "Infinite Jump", invisibleEnabled = "Invisible (Ghost)",
    speedEnabled = "Speed Changer", jumpEnabled = "Jump Power",
    freeze = "Freeze (Anchor)", fullbright = "Fullbright",
    fovEnabled = "FOV Changer", clickTP = "Click / Tap Teleport",
    instantInteract = "Instant Interact",
    cameraZoomEnabled = "Camera Max Zoom",
    infoHudEnabled = "Info HUD",
}
local notifyExcluded = {}

saveSettings = function()
    pcall(function()
        if writefile then
            ensureConfigFolder()
            writefile(getFullPath(), HttpService:JSONEncode({ state = state, config = settingsConfig }))
        end
    end)
end

local function loadSettings()
    pcall(function()
        local path = getFullPath()
        if readfile and isfile and isfile(path) then
            local decoded = HttpService:JSONDecode(readfile(path))
            if decoded.state then
                for k, v in pairs(decoded.state) do if state[k] ~= nil then state[k] = v end end
            end
            if decoded.config then
                for k, v in pairs(decoded.config) do if settingsConfig[k] ~= nil then settingsConfig[k] = v end end
            end
            if settingsConfig.tweenSpeed then TWEEN_SPEED = settingsConfig.tweenSpeed end
            if settingsConfig.roundLength then ROUND_LENGTH_FALLBACK = settingsConfig.roundLength end
            if type(settingsConfig.flingList) ~= "table" then settingsConfig.flingList = {} end
            if type(settingsConfig.whitelist) ~= "table" then settingsConfig.whitelist = {} end
        end
    end)
    state.antiRagdoll = true
end
loadSettings()

local function setStatus(msg)
    if not st then return end
    st.Text = "  " .. tostring(msg)
    st.TextColor3 = Color3.fromRGB(255, 255, 255)
    task.spawn(function()
        TweenService:Create(st, TweenInfo.new(0.1), {TextColor3 = Color3.fromRGB(180, 180, 180)}):Play()
        task.wait(0.1)
        if st then TweenService:Create(st, TweenInfo.new(0.25), {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play() end
    end)
end

-- ==================== ANIMATION HELPERS ====================
local function attachClickBounce(btn)
    local us = Instance.new("UIScale", btn)
    us.Scale = 1
    btn.MouseButton1Down:Connect(function() TweenService:Create(us, TweenInfo.new(0.08, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Scale = 0.92}):Play() end)
    local function restore() TweenService:Create(us, TweenInfo.new(0.45, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Scale = 1}):Play() end
    btn.MouseButton1Up:Connect(restore); btn.MouseLeave:Connect(restore)
end

local function attachHoverLift(btn, amount)
    amount = amount or 1.03
    local us = Instance.new("UIScale", btn); us.Scale = 1
    btn.MouseEnter:Connect(function() TweenService:Create(us, TweenInfo.new(0.28, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = amount}):Play() end)
    btn.MouseLeave:Connect(function() TweenService:Create(us, TweenInfo.new(0.35, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Scale = 1}):Play() end)
end

local function attachHoverTilt(btn, degrees)
    degrees = degrees or 1.5
    local savedRotation = btn.Rotation or 0
    btn.MouseEnter:Connect(function() TweenService:Create(btn, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Rotation = savedRotation + degrees}):Play() end)
    btn.MouseLeave:Connect(function() TweenService:Create(btn, TweenInfo.new(0.35, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Rotation = savedRotation}):Play() end)
end

local function attachRipple(btn, color)
    color = color or Color3.fromRGB(255, 255, 255)
    btn.ClipsDescendants = true
    btn.MouseButton1Down:Connect(function()
        local m = UIS:GetMouseLocation()
        local relX = m.X - btn.AbsolutePosition.X
        local relY = m.Y - btn.AbsolutePosition.Y
        local ripple = Instance.new("Frame")
        ripple.Size = UDim2.new(0, 0, 0, 0)
        ripple.Position = UDim2.new(0, relX, 0, relY)
        ripple.AnchorPoint = Vector2.new(0.5, 0.5)
        ripple.BackgroundColor3 = color
        ripple.BackgroundTransparency = 0.55
        ripple.BorderSizePixel = 0
        ripple.ZIndex = (btn.ZIndex or 1) + 5
        ripple.Parent = btn
        Instance.new("UICorner", ripple).CornerRadius = UDim.new(1, 0)
        local maxDim = math.max(btn.AbsoluteSize.X, btn.AbsoluteSize.Y) * 2.5
        TweenService:Create(ripple, TweenInfo.new(0.55, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, maxDim, 0, maxDim),
            BackgroundTransparency = 1
        }):Play()
        task.delay(0.6, function() if ripple and ripple.Parent then ripple:Destroy() end end)
    end)
end

local function attachShine(btn, duration)
    duration = duration or 0.55
    btn.ClipsDescendants = true
    local shine = Instance.new("Frame")
    shine.Name = "AW_Shine"
    shine.Size = UDim2.new(0, 28, 1.6, 0)
    shine.Position = UDim2.new(-0.35, 0, -0.3, 0)
    shine.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    shine.BackgroundTransparency = 0.88
    shine.Rotation = 14
    shine.BorderSizePixel = 0
    shine.ZIndex = (btn.ZIndex or 1) + 2
    shine.Parent = btn
    btn.MouseEnter:Connect(function()
        shine.Position = UDim2.new(-0.35, 0, -0.3, 0)
        TweenService:Create(shine, TweenInfo.new(duration, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Position = UDim2.new(1.35, 0, -0.3, 0)
        }):Play()
    end)
end

local function attachAnimatedGradient(frame, color1, color2, speed)
    speed = speed or 2
    local grad = Instance.new("UIGradient")
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, color1),
        ColorSequenceKeypoint.new(0.5, color2),
        ColorSequenceKeypoint.new(1, color1)
    })
    grad.Parent = frame
    task.spawn(function()
        local t = 0
        while grad.Parent do
            t = t + 0.035
            grad.Offset = Vector2.new(math.sin(t * speed) * 0.35, 0)
            task.wait(0.035)
        end
    end)
    return grad
end

local function staggerFadeIn(page)
    local children = {}
    for _, c in ipairs(page:GetChildren()) do if c:IsA("GuiObject") and c.Visible ~= false then table.insert(children, c) end end
    for i, c in ipairs(children) do
        local savedBg = c.BackgroundTransparency
        local savedTextTr = nil
        local savedPos = c.Position
        local offsetX = 24 * (settingsConfig.selectedScale or 1)
        if c:IsA("TextLabel") or c:IsA("TextButton") or c:IsA("TextBox") then savedTextTr = c.TextTransparency; c.TextTransparency = 1 end
        if c:IsA("Frame") or c:IsA("TextButton") or c:IsA("TextBox") then c.BackgroundTransparency = 1 end
        c.Position = UDim2.new(savedPos.X.Scale, savedPos.X.Offset + offsetX, savedPos.Y.Scale, savedPos.Y.Offset)
        task.delay(i * 0.024, function()
            if not c.Parent then return end
            if c:IsA("Frame") or c:IsA("TextButton") or c:IsA("TextBox") then
                TweenService:Create(c, TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {BackgroundTransparency = savedBg}):Play()
            end
            if savedTextTr ~= nil then TweenService:Create(c, TweenInfo.new(0.42, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {TextTransparency = savedTextTr}):Play() end
            TweenService:Create(c, TweenInfo.new(0.42, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Position = savedPos}):Play()
        end)
    end
end

local function startPulsingGlow(glowFrame, isOn)
    task.spawn(function()
        while glowFrame.Parent do
            if isOn() then
                TweenService:Create(glowFrame, TweenInfo.new(1.2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {BackgroundTransparency = 0.55}):Play()
                task.wait(1.2)
                if not glowFrame.Parent then break end
                TweenService:Create(glowFrame, TweenInfo.new(1.2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {BackgroundTransparency = 0.92}):Play()
                task.wait(1.2)
            else task.wait(0.5) end
        end
    end)
end

-- ==================== NOTIFICATIONS ====================
local notifyGui = Instance.new("ScreenGui")
notifyGui.Name = "ApplewareNotifications"
notifyGui.Parent = getGuiParent()
notifyGui.ResetOnSpawn = false
notifyGui.DisplayOrder = 999998
notifyGui.IgnoreGuiInset = true

local notifyContainer = Instance.new("Frame")
notifyContainer.Name = "NotifyContainer"
notifyContainer.AnchorPoint = Vector2.new(0, 1)
notifyContainer.Position = UDim2.new(0, 14, 1, -14)
notifyContainer.AutomaticSize = Enum.AutomaticSize.Y
notifyContainer.Size = UDim2.new(0, 310, 0, 0)
notifyContainer.BackgroundTransparency = 1
notifyContainer.Active = false
notifyContainer.Parent = notifyGui

local notifyLayout = Instance.new("UIListLayout")
notifyLayout.Padding = UDim.new(0, 8)
notifyLayout.SortOrder = Enum.SortOrder.LayoutOrder
notifyLayout.VerticalAlignment = Enum.VerticalAlignment.Bottom
notifyLayout.Parent = notifyContainer

local notificationCount = 0
local MAX_NOTIFICATIONS = 5
local NOTIFICATION_DURATION = 5

local function showNotification(bodyRichText)
    if notificationCount >= MAX_NOTIFICATIONS then return end
    notificationCount = notificationCount + 1
    local wrapper = Instance.new("Frame")
    wrapper.Size = UDim2.new(1, 0, 0, 60)
    wrapper.BackgroundTransparency = 1
    wrapper.LayoutOrder = math.floor(tick() * 1000)
    wrapper.Parent = notifyContainer

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.85, 0, 0.9, 0)
    frame.Position = UDim2.new(-1.15, 0, 0.05, 0)
    frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    frame.BackgroundTransparency = 0.08
    frame.BorderSizePixel = 0
    frame.ClipsDescendants = true
    frame.Parent = wrapper
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)

    local grad = Instance.new("UIGradient")
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(14, 14, 14)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0))
    })
    grad.Rotation = 90; grad.Parent = frame

    local stroke = Instance.new("UIStroke", frame)
    stroke.Color = Color3.fromRGB(90, 90, 90); stroke.Thickness = 1; stroke.Transparency = 0.3

    local accent = Instance.new("Frame")
    accent.Size = UDim2.new(0, 3, 0, 0)
    accent.Position = UDim2.new(0, 6, 0.5, 0)
    accent.AnchorPoint = Vector2.new(0, 0.5)
    accent.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    accent.Parent = frame
    Instance.new("UICorner", accent).CornerRadius = UDim.new(1, 0)

    local iconBg = Instance.new("Frame")
    iconBg.Size = UDim2.new(0, 0, 0, 0)
    iconBg.Position = UDim2.new(0, 29, 0.5, 0)
    iconBg.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    iconBg.Parent = frame
    Instance.new("UICorner", iconBg).CornerRadius = UDim.new(1, 0)

    local iconLbl = Instance.new("TextLabel")
    iconLbl.Size = UDim2.new(1, 0, 1, 0); iconLbl.BackgroundTransparency = 1
    iconLbl.Text = "i"; iconLbl.TextColor3 = Color3.fromRGB(0, 0, 0)
    iconLbl.Font = Enum.Font.GothamBold; iconLbl.TextSize = 16
    iconLbl.Rotation = -180; iconLbl.Parent = iconBg

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -60, 0, 18); title.Position = UDim2.new(0, 52, 0, 10)
    title.BackgroundTransparency = 1; title.Text = "Appleware Says:"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.Font = Enum.Font.GothamBold; title.TextSize = 14
    title.TextXAlignment = Enum.TextXAlignment.Left; title.TextTransparency = 1; title.Parent = frame

    local body = Instance.new("TextLabel")
    body.Size = UDim2.new(1, -60, 0, 18); body.Position = UDim2.new(0, 52, 0, 30)
    body.BackgroundTransparency = 1; body.Text = bodyRichText
    body.TextColor3 = Color3.fromRGB(255, 255, 255)
    body.Font = Enum.Font.Gotham; body.TextSize = 12
    body.TextXAlignment = Enum.TextXAlignment.Left; body.RichText = true; body.TextTransparency = 1; body.Parent = frame

    local barBg = Instance.new("Frame")
    barBg.Size = UDim2.new(1, -28, 0, 3); barBg.Position = UDim2.new(0, 22, 1, -8)
    barBg.BackgroundColor3 = Color3.fromRGB(35, 35, 35); barBg.Parent = frame
    Instance.new("UICorner", barBg).CornerRadius = UDim.new(1, 0)

    local bar = Instance.new("Frame")
    bar.Size = UDim2.new(1, 0, 1, 0); bar.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    bar.Parent = barBg
    Instance.new("UICorner", bar).CornerRadius = UDim.new(1, 0)

    TweenService:Create(frame, TweenInfo.new(0.65, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Position = UDim2.new(0, 0, 0, 0), Size = UDim2.new(1, 0, 1, 0)
    }):Play()
    TweenService:Create(iconBg, TweenInfo.new(0.65, Enum.EasingStyle.Back, Enum.EasingDirection.Out, 0, false, 0.15), {
        Size = UDim2.new(0, 26, 0, 26), Position = UDim2.new(0, 16, 0.5, -13)
    }):Play()
    TweenService:Create(iconLbl, TweenInfo.new(0.9, Enum.EasingStyle.Back, Enum.EasingDirection.Out, 0, false, 0.15), {Rotation = 0}):Play()
    TweenService:Create(accent, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out, 0, false, 0.1), {Size = UDim2.new(0, 3, 0, 30)}):Play()
    TweenService:Create(title, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out, 0, false, 0.2), {TextTransparency = 0}):Play()
    body.Position = UDim2.new(0, 52, 0, 38)
    TweenService:Create(body, TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out, 0, false, 0.28), {TextTransparency = 0, Position = UDim2.new(0, 52, 0, 30)}):Play()
    TweenService:Create(bar, TweenInfo.new(NOTIFICATION_DURATION, Enum.EasingStyle.Linear, Enum.EasingDirection.Out, 0, false, 0.4), {Size = UDim2.new(0, 0, 1, 0)}):Play()

    task.delay(NOTIFICATION_DURATION, function()
        if not wrapper.Parent then return end
        TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
            Position = UDim2.new(-1.2, 0, 0, 0), Size = UDim2.new(0.85, 0, 0.9, 0), BackgroundTransparency = 1
        }):Play()
        TweenService:Create(title, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
        TweenService:Create(body, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
        TweenService:Create(iconBg, TweenInfo.new(0.3), {BackgroundTransparency = 1, Size = UDim2.new(0, 0, 0, 0)}):Play()
        TweenService:Create(iconLbl, TweenInfo.new(0.3), {TextTransparency = 1, Rotation = 180}):Play()
        TweenService:Create(accent, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
        TweenService:Create(bar, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
        TweenService:Create(barBg, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
        TweenService:Create(stroke, TweenInfo.new(0.3), {Transparency = 1}):Play()
        task.wait(0.5)
        if wrapper and wrapper.Parent then wrapper:Destroy() end
        notificationCount = math.max(0, notificationCount - 1)
    end)
end

-- ==================== ROUND TIMER ====================
local roundTimerGui = Instance.new("ScreenGui")
roundTimerGui.Name = "ApplewareRoundTimer"
roundTimerGui.Parent = getGuiParent()
roundTimerGui.ResetOnSpawn = false
roundTimerGui.DisplayOrder = 999997
roundTimerGui.IgnoreGuiInset = true

local roundTimerFrame = Instance.new("Frame")
roundTimerFrame.AnchorPoint = Vector2.new(0.5, 0)
roundTimerFrame.Position = UDim2.new(0.5, 0, 0, 8)
roundTimerFrame.Size = UDim2.new(0, 132, 0, 54)
roundTimerFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
roundTimerFrame.BackgroundTransparency = 0.1
roundTimerFrame.Visible = false
roundTimerFrame.Parent = roundTimerGui
Instance.new("UICorner", roundTimerFrame).CornerRadius = UDim.new(0, 10)

local rtStroke = Instance.new("UIStroke", roundTimerFrame)
rtStroke.Color = Color3.fromRGB(60, 60, 60); rtStroke.Thickness = 1; rtStroke.Transparency = 0.3

local rtTitle = Instance.new("TextLabel")
rtTitle.Size = UDim2.new(1, 0, 0, 14); rtTitle.Position = UDim2.new(0, 0, 0, 4)
rtTitle.BackgroundTransparency = 1; rtTitle.Text = "ROUND TIME"
rtTitle.TextColor3 = Color3.fromRGB(150, 150, 150)
rtTitle.Font = Enum.Font.GothamBold; rtTitle.TextSize = 10; rtTitle.Parent = roundTimerFrame

local rtValue = Instance.new("TextLabel")
rtValue.Size = UDim2.new(1, 0, 0, 28); rtValue.Position = UDim2.new(0, 0, 0, 20)
rtValue.BackgroundTransparency = 1; rtValue.Text = "0:00"
rtValue.TextColor3 = Color3.fromRGB(255, 255, 255)
rtValue.Font = Enum.Font.GothamBold; rtValue.TextSize = 22
rtValue.TextStrokeTransparency = 1; rtValue.Parent = roundTimerFrame

-- ==================== INFO HUD ====================
local infoHudGui = Instance.new("ScreenGui")
infoHudGui.Name = "ApplewareInfoHud"
infoHudGui.Parent = getGuiParent()
infoHudGui.ResetOnSpawn = false
infoHudGui.DisplayOrder = 999996
infoHudGui.IgnoreGuiInset = true

local infoHudFrame = Instance.new("Frame")
infoHudFrame.AnchorPoint = Vector2.new(1, 0)
infoHudFrame.Position = UDim2.new(1, -14, 0, 14)
infoHudFrame.Size = UDim2.new(0, 220, 0, 158)
infoHudFrame.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
infoHudFrame.BackgroundTransparency = 0.08
infoHudFrame.Visible = false
infoHudFrame.Parent = infoHudGui
Instance.new("UICorner", infoHudFrame).CornerRadius = UDim.new(0, 12)
local ihStroke = Instance.new("UIStroke", infoHudFrame)
ihStroke.Color = Color3.fromRGB(50, 50, 50); ihStroke.Thickness = 1; ihStroke.Transparency = 0.25
local ihGrad = Instance.new("UIGradient")
ihGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 20, 20)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0))
})
ihGrad.Rotation = 90
ihGrad.Parent = infoHudFrame

local ihDot = Instance.new("Frame")
ihDot.Size = UDim2.new(0, 8, 0, 8)
ihDot.Position = UDim2.new(0, 12, 0, 13)
ihDot.AnchorPoint = Vector2.new(0.5, 0.5)
ihDot.BackgroundColor3 = Color3.fromRGB(90, 255, 140)
ihDot.Parent = infoHudFrame
Instance.new("UICorner", ihDot).CornerRadius = UDim.new(1, 0)
local ihDotGlow = Instance.new("Frame")
ihDotGlow.Size = UDim2.new(0, 8, 0, 8)
ihDotGlow.Position = UDim2.new(0, 12, 0, 13)
ihDotGlow.AnchorPoint = Vector2.new(0.5, 0.5)
ihDotGlow.BackgroundColor3 = Color3.fromRGB(90, 255, 140)
ihDotGlow.BackgroundTransparency = 0.7
ihDotGlow.ZIndex = -1
ihDotGlow.Parent = infoHudFrame
Instance.new("UICorner", ihDotGlow).CornerRadius = UDim.new(1, 0)

local ihHeader = Instance.new("TextLabel")
ihHeader.Size = UDim2.new(1, -30, 0, 22)
ihHeader.Position = UDim2.new(0, 24, 0, 2)
ihHeader.BackgroundTransparency = 1
ihHeader.Text = "APPLEWARE"
ihHeader.TextColor3 = Color3.fromRGB(240, 240, 240)
ihHeader.Font = Enum.Font.GothamBold
ihHeader.TextSize = 11
ihHeader.TextXAlignment = Enum.TextXAlignment.Left
ihHeader.Parent = infoHudFrame

local ihSub = Instance.new("TextLabel")
ihSub.Size = UDim2.new(1, -30, 0, 10)
ihSub.Position = UDim2.new(0, 24, 0, 16)
ihSub.BackgroundTransparency = 1
ihSub.Text = "Session Active"
ihSub.TextColor3 = Color3.fromRGB(120, 120, 120)
ihSub.Font = Enum.Font.Gotham
ihSub.TextSize = 9
ihSub.TextXAlignment = Enum.TextXAlignment.Left
ihSub.Parent = infoHudFrame

local ihLine = Instance.new("Frame")
ihLine.Size = UDim2.new(1, -20, 0, 1)
ihLine.Position = UDim2.new(0, 10, 0, 30)
ihLine.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
ihLine.BackgroundTransparency = 0.4
ihLine.Parent = infoHudFrame

task.spawn(function()
    while ihDot.Parent do
        TweenService:Create(ihDot, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {BackgroundTransparency = 0.4}):Play()
        TweenService:Create(ihDotGlow, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Size = UDim2.new(0, 16, 0, 16), BackgroundTransparency = 0.9}):Play()
        task.wait(1)
        if not ihDot.Parent then break end
        TweenService:Create(ihDot, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {BackgroundTransparency = 0}):Play()
        TweenService:Create(ihDotGlow, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Size = UDim2.new(0, 8, 0, 8), BackgroundTransparency = 0.7}):Play()
        task.wait(1)
    end
end)

local function makeInfoRow(y, label)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.55, -8, 0, 18)
    lbl.Position = UDim2.new(0, 12, 0, y)
    lbl.BackgroundTransparency = 1
    lbl.Text = label
    lbl.TextColor3 = Color3.fromRGB(140, 140, 140)
    lbl.Font = Enum.Font.GothamMedium
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = infoHudFrame
    local val = Instance.new("TextLabel")
    val.Size = UDim2.new(0.45, -8, 0, 18)
    val.Position = UDim2.new(0.55, 0, 0, y)
    val.BackgroundTransparency = 1
    val.Text = "-"
    val.TextColor3 = Color3.fromRGB(245, 245, 245)
    val.Font = Enum.Font.GothamBold
    val.TextSize = 11
    val.TextXAlignment = Enum.TextXAlignment.Right
    val.Parent = infoHudFrame
    return val
end

local ihTime = makeInfoRow(40, "Session")
local ihPlayers = makeInfoRow(60, "Players")
local ihRole = makeInfoRow(80, "Role")
local ihPing = makeInfoRow(100, "Ping")
local ihFps = makeInfoRow(120, "FPS")

local hudSessionStart = tick()
local fpsSamples = {}
local currentFps = 60

RunService.RenderStepped:Connect(function(dt)
    if dt and dt > 0 then
        table.insert(fpsSamples, 1 / dt)
        if #fpsSamples > 30 then table.remove(fpsSamples, 1) end
    end
end)

task.spawn(function()
    while true do
        task.wait(0.35)
        if state.infoHudEnabled then
            if not infoHudFrame.Visible then infoHudFrame.Visible = true end
            local elapsed = math.floor(tick() - hudSessionStart)
            local h = math.floor(elapsed / 3600)
            local m = math.floor((elapsed % 3600) / 60)
            local s = elapsed % 60
            ihTime.Text = string.format("%02d:%02d:%02d", h, m, s)
            ihPlayers.Text = tostring(#Players:GetPlayers())
            local role = "Innocent"
            pcall(function()
                if lpHasGun and lpHasGun() then role = "Sheriff"
                else
                    local bp, ch = LP:FindFirstChild("Backpack"), LP.Character
                    if (bp and bp:FindFirstChild("Knife")) or (ch and ch:FindFirstChild("Knife")) then role = "Murderer" end
                end
            end)
            ihRole.Text = role
            if role == "Murderer" then ihRole.TextColor3 = Color3.fromRGB(255, 90, 90)
            elseif role == "Sheriff" then ihRole.TextColor3 = Color3.fromRGB(90, 170, 255)
            else ihRole.TextColor3 = Color3.fromRGB(120, 255, 140) end

            pcall(function()
                local ping = StatsService.Network.ServerStatsItem["Data Ping"]:GetValue()
                ping = math.floor(ping)
                ihPing.Text = ping .. " ms"
                if ping < 80 then ihPing.TextColor3 = Color3.fromRGB(120, 255, 140)
                elseif ping < 150 then ihPing.TextColor3 = Color3.fromRGB(255, 200, 80)
                else ihPing.TextColor3 = Color3.fromRGB(255, 100, 100) end
            end)

            if #fpsSamples > 0 then
                local sum = 0
                for _, v in ipairs(fpsSamples) do sum = sum + v end
                currentFps = math.floor(sum / #fpsSamples + 0.5)
            end
            ihFps.Text = tostring(currentFps)
            if currentFps >= 50 then ihFps.TextColor3 = Color3.fromRGB(120, 255, 140)
            elseif currentFps >= 30 then ihFps.TextColor3 = Color3.fromRGB(255, 200, 80)
            else ihFps.TextColor3 = Color3.fromRGB(255, 100, 100) end
        else
            if infoHudFrame.Visible then infoHudFrame.Visible = false end
        end
    end
end)

-- ==================== WATERPROOF ====================
local table_insert = table.insert
local nicolas = {}
nicolas.__index = nicolas
function nicolas.new() return setmetatable({_tasks = {}, _destroyed = false}, nicolas) end
function nicolas:GiveTask(t)
    if self._destroyed then self:_cleanupTask(t) return end
    table_insert(self._tasks, t); return t
end
function nicolas:GiveTasks(...) for _, t in ipairs({...}) do self:GiveTask(t) end end
function nicolas:_cleanupTask(t)
    local ty = typeof(t)
    if ty == "RBXScriptConnection" then t:Disconnect()
    elseif ty == "Instance" then t:Destroy()
    elseif ty == "function" then t()
    elseif ty == "table" and type(t.Destroy) == "function" then t:Destroy() end
end
function nicolas:DoCleaning()
    if self._destroyed then return end
    self._destroyed = true
    for _, t in ipairs(self._tasks) do self:_cleanupTask(t) end
    self._tasks = {}
end
function nicolas:Destroy() self:DoCleaning() end

local waterMaid = nil
local modifiedParts = {}
local function DisableWaterPart(part)
    if part and part:IsA("BasePart") then
        if not modifiedParts[part] then modifiedParts[part] = {CanTouch = part.CanTouch, CanCollide = part.CanCollide} end
        part.CanTouch = false; part.CanCollide = false
    end
end
local function CheckMaps()
    local yacht = Workspace:FindFirstChild("Yacht")
    if yacht then
        local inter = yacht:FindFirstChild("Intereactive")
        if inter then
            local water = inter:FindFirstChild("Water")
            if water then DisableWaterPart(water:FindFirstChild("WaterPart")) end
        end
    end
    local pier = Workspace:FindFirstChild("Pier")
    if pier then DisableWaterPart(pier:FindFirstChild("Respawn")) end
end
local function enableWaterImmunity()
    if waterMaid then waterMaid:DoCleaning() waterMaid = nil end
    waterMaid = nicolas.new()
    CheckMaps()
    waterMaid:GiveTask(Workspace.DescendantAdded:Connect(CheckMaps))
    waterMaid:GiveTask(Workspace.DescendantRemoved:Connect(CheckMaps))
end
task.spawn(enableWaterImmunity)

local selectedScale = settingsConfig.selectedScale or 1
local mainToggleButtons = {}
local quickToggleButtons = {}
local quickFrameReference = nil
local refreshQuickTogglesPanel = nil
local refreshQuickManagerFn = nil
local refreshPlayerPickerFn = nil

local function applyLowDeviceOptimizations(enabled) end
local function applyFpsBoost(enabled) end

local function updateAllToggleVisuals(key)
    if mainToggleButtons[key] then
        local data = mainToggleButtons[key]
        local on = state[key]
        TweenService:Create(data.pill, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            BackgroundColor3 = on and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(32, 32, 32)
        }):Play()
        TweenService:Create(data.dot, TweenInfo.new(0.7, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {
            Position = on and UDim2.new(1, -20 * selectedScale, 0.5, -8 * selectedScale) or UDim2.new(0, 4 * selectedScale, 0.5, -8 * selectedScale),
            BackgroundColor3 = on and Color3.fromRGB(0, 0, 0) or Color3.fromRGB(200, 200, 200)
        }):Play()
        if data.inner then TweenService:Create(data.inner, TweenInfo.new(0.35, Enum.EasingStyle.Quint), {
            BackgroundColor3 = on and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(22, 22, 22)
        }):Play() end
        if data.glow then TweenService:Create(data.glow, TweenInfo.new(0.35), {
            BackgroundTransparency = on and 0.78 or 1
        }):Play() end
        if data.pillScale then
            data.pillScale.Scale = 1.15
            TweenService:Create(data.pillScale, TweenInfo.new(0.6, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Scale = 1}):Play()
        end
    end
    if quickToggleButtons[key] then
        local on = state[key]
        TweenService:Create(quickToggleButtons[key], TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            BackgroundColor3 = on and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(18, 18, 18),
            TextColor3 = on and Color3.fromRGB(0, 0, 0) or Color3.fromRGB(220, 220, 220)
        }):Play()
    end
end

local function setToggleState(key, value)
    if key == "antiRagdoll" then value = true end
    local prev = state[key]
    state[key] = value
    if prev ~= value and not notifyExcluded[key] then
        local displayName = toggleDisplayNames[key] or key
        local color = value and "#5aff8c" or "#ff5a5a"
        local verb = value and "enabled" or "disabled"
        showNotification('<font color="' .. color .. '">' .. displayName .. '</font> ' .. verb)
    end
    if value then
        if key == "aimbotMurderer" then state.aimbotSheriff = false; updateAllToggleVisuals("aimbotSheriff")
        elseif key == "aimbotSheriff" then state.aimbotMurderer = false; updateAllToggleVisuals("aimbotMurderer")
        elseif key == "farm" then state.xpFarm = false; updateAllToggleVisuals("xpFarm")
        elseif key == "xpFarm" then state.farm = false; updateAllToggleVisuals("farm") end
    end
    updateAllToggleVisuals(key)
    if key == "disable3d" then applyLowDeviceOptimizations(state.disable3d)
    elseif key == "fullbright" then
        pcall(function()
            if state.fullbright then
                Lighting.Brightness = 3
                Lighting.Ambient = Color3.fromRGB(178, 178, 178)
                Lighting.OutdoorAmbient = Color3.fromRGB(178, 178, 178)
                Lighting.FogEnd = 100000
            else
                Lighting.Brightness = 1
                Lighting.Ambient = Color3.fromRGB(70, 70, 70)
                Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
            end
        end)
    elseif key == "fpsBoost" then applyFpsBoost(state.fpsBoost)
    elseif key == "quickTogglesVisible" and quickFrameReference then quickFrameReference.Visible = state.quickTogglesVisible
    elseif key == "showRoundTimerCountdown" then if not value then roundTimerFrame.Visible = false end end
    saveSettings()
end

local UNDER = 4.8
local HIDE_POS = CFrame.new(0, -300, 0)
local MIN_BAG_FULL = 40
local lastCoinFoundTime = tick()
local NO_COIN_TIMEOUT = 12
local isExecutingAction = false
local currentCoinCount = 0
local maxCoinCount = 40
local bagFull = false
local busy = false
local currentFarmTween = nil
local currentTargetCoin = nil
local farmVelocityConn = nil
local totalCoinsEarned = 0
local sessionStartTime = tick()
local lastRoundState = false
local roundStartTime = 0
local hasCollectedThisRound = false
local roundFullyStarted = false

-- ==================== FLING ====================
local activeResets = {}
local MAX_CONCURRENT_RESETS = 6

local function countActiveResets()
    local c = 0; for _ in pairs(activeResets) do c = c + 1 end; return c
end

local function targetIsAlive(plr)
    if not plr or not plr.Parent then return false end
    local c = plr.Character; if not c then return false end
    local h = c:FindFirstChildOfClass("Humanoid"); if not h or h.Health <= 0 then return false end
    return true
end

local function touch(a, b)
    pcall(function() firetouchinterest(a, b, 0); firetouchinterest(a, b, 1) end)
end

local function fullyRestoreCharacter(character, savedData)
    if not character or not savedData then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if not humanoid or not rootPart then return end
    humanoid.PlatformStand = false
    for _, track in ipairs(humanoid:GetPlayingAnimationTracks()) do track:Stop() end
    rootPart.AssemblyLinearVelocity = Vector3.zero
    rootPart.AssemblyAngularVelocity = Vector3.zero
    rootPart.Velocity = Vector3.zero; rootPart.RotVelocity = Vector3.zero
    rootPart.CFrame = savedData.cframe
    pcall(sethiddenproperty, rootPart, "PhysicsRepRootPart", rootPart)
    humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") then part.CanCollide = true end
    end
end

local function resetPlayer(TargetPlayer)
    if not TargetPlayer or TargetPlayer == LP then return false end
    if not targetIsAlive(TargetPlayer) then return false end
    if activeResets[TargetPlayer.UserId] then return false end
    if countActiveResets() >= MAX_CONCURRENT_RESETS then
        task.defer(function() task.wait(0.1); resetPlayer(TargetPlayer) end)
        return false
    end
    local Character = LP.Character; if not Character then return false end
    local Humanoid = Character:FindFirstChildOfClass("Humanoid")
    local RootPart = Humanoid and Humanoid.RootPart
    local TCharacter = TargetPlayer.Character
    if not (Character and Humanoid and RootPart and TCharacter) then return false end
    local TRootPart = TCharacter:FindFirstChild("HumanoidRootPart")
    local THead = TCharacter:FindFirstChild("Head")
    if not TRootPart then return false end

    local savedData = { cframe = RootPart.CFrame }
    Humanoid.PlatformStand = true

    local bv = Instance.new("BodyVelocity")
    bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bv.Velocity = Vector3.new(0, -50000, 0)
    bv.Parent = RootPart
    local bg = Instance.new("BodyGyro")
    bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    bg.P = 1000000; bg.Parent = RootPart

    local originalDestroyHeight = Workspace.FallenPartsDestroyHeight
    Workspace.FallenPartsDestroyHeight = -100000

    local startTime = tick()
    local resetObj = { active = true }
    activeResets[TargetPlayer.UserId] = resetObj
    setStatus("Flinging " .. TargetPlayer.Name .. "...")

    local conn
    conn = RunService.Heartbeat:Connect(function()
        if tick() - startTime > RESET_DURATION or not TargetPlayer.Character or not TRootPart.Parent then
            Workspace.FallenPartsDestroyHeight = originalDestroyHeight
            if bv.Parent then bv:Destroy() end
            if bg.Parent then bg:Destroy() end
            pcall(sethiddenproperty, RootPart, "PhysicsRepRootPart", RootPart)
            fullyRestoreCharacter(Character, savedData)
            if conn then conn:Disconnect() end
            resetObj.active = false
            activeResets[TargetPlayer.UserId] = nil
            if targetIsAlive(TargetPlayer) then setStatus("Fling finished - " .. TargetPlayer.Name .. " survived")
            else setStatus("Target " .. TargetPlayer.Name .. " flung!") end
            return
        end
        if TRootPart and TRootPart.Parent and Character and Character.Parent then
            local slamPos = TRootPart.Position + Vector3.new(0, 1, 0)
            RootPart.CFrame = CFrame.new(slamPos) * CFrame.Angles(math.pi / 2, 0, 0)
            RootPart.AssemblyLinearVelocity = Vector3.new(0, -50000, 0)
            RootPart.AssemblyAngularVelocity = Vector3.new(7500, 7500, 7500)
            for i = 1, 10 do
                touch(RootPart, TRootPart)
                if THead then touch(RootPart, THead) end
            end
            pcall(sethiddenproperty, RootPart, "PhysicsRepRootPart", TRootPart)
        end
    end)
    return true
end

local function reliableFling(TargetPlayer)
    if not TargetPlayer or TargetPlayer == LP or not TargetPlayer.Parent then return end
    if not targetIsAlive(TargetPlayer) then return end
    if activeResets[TargetPlayer.UserId] then return end
    resetPlayer(TargetPlayer)
    local timeout = tick() + RESET_DURATION + 1.5
    while activeResets[TargetPlayer.UserId] and tick() < timeout do task.wait(0.05) end
end

-- ==================== CORE ====================
local function isInRound()
    for _, map in ipairs(Workspace:GetChildren()) do
        if map:FindFirstChild("CoinContainer") or map.Name == "CoinContainer" then return true end
    end
    return false
end
local function isLobby() return not isInRound() end

local function getMapName()
    for _, map in ipairs(Workspace:GetChildren()) do
        if map:FindFirstChild("CoinContainer") or map.Name == "CoinContainer" then return map.Name end
    end
    return nil
end

local function getRoundTimeRemaining()
    local sources = {Workspace, ReplicatedStorage}
    local names = {"RoundTimer", "TimeLeft", "RoundTime", "TimeRemaining", "RoundCountdown", "Timer"}
    for _, src in ipairs(sources) do
        for _, name in ipairs(names) do
            local v = src:FindFirstChild(name)
            if v and (v:IsA("IntValue") or v:IsA("NumberValue")) then
                local val = tonumber(v.Value)
                if val and val > 0 and val < 900 then return val end
            end
        end
    end
    if roundFullyStarted and roundStartTime > 0 then
        return math.max(0, ROUND_LENGTH_FALLBACK - (tick() - roundStartTime))
    end
    return nil
end

local function getCachedRoleData()
    local ok, data = pcall(function()
        local modules = ReplicatedStorage:FindFirstChild("Modules")
        if modules and modules:FindFirstChild("PlayerData") then return require(modules.PlayerData) end
    end)
    if ok and type(data) == "table" then return data end
    local remote = ReplicatedStorage:FindFirstChild("GetPlayerData", true)
    if remote and remote:IsA("RemoteFunction") then
        local success, res = pcall(function() return remote:InvokeServer() end)
        if success and type(res) == "table" then return res end
    end
    return nil
end

local function getPlayerRole(plr)
    if not plr or not plr.Character then return "Innocent" end
    local bp, char = plr:FindFirstChild("Backpack"), plr.Character
    if (bp and bp:FindFirstChild("Knife")) or (char and char:FindFirstChild("Knife")) then return "Murderer" end
    if (bp and bp:FindFirstChild("Gun")) or (char and char:FindFirstChild("Gun")) then return "Sheriff" end
    local roleData = getCachedRoleData()
    if roleData then
        local pData = roleData[plr.Name] or roleData[tostring(plr.UserId)]
        if type(pData) == "table" then
            local role = pData.Role or pData.role or pData.RoleName
            if role and role ~= "" then return role end
        end
    end
    return "Innocent"
end

local function findPlayerByRole(roleName)
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0 then
            if getPlayerRole(plr) == roleName then return plr end
        end
    end
    return nil
end

local function getPlayerPerk(plr)
    if not plr then return nil end
    local perk = plr:GetAttribute("Perk") or plr:GetAttribute("EquippedPerk") or plr:GetAttribute("PerkName")
    if perk then return tostring(perk) end
    if plr.Character then
        perk = plr.Character:GetAttribute("Perk") or plr.Character:GetAttribute("EquippedPerk")
        if perk then return tostring(perk) end
        local perkPart = plr.Character:FindFirstChild("Perk") or plr.Character:FindFirstChild("PerkTag")
        if perkPart then return perkPart.Name end
    end
    local bp = plr:FindFirstChild("Backpack")
    if bp then
        local perkTool = bp:FindFirstChild("Perk") or bp:FindFirstChild("PerkTag")
        if perkTool then return perkTool.Name end
    end
    return nil
end

local function getMurd()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0 then
            local bp, char = plr:FindFirstChild("Backpack"), plr.Character
            if (bp and bp:FindFirstChild("Knife")) or (char and char:FindFirstChild("Knife")) or getPlayerRole(plr) == "Murderer" then return plr end
        end
    end
    return nil
end

local function getSheriff()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0 then
            local bp, char = plr:FindFirstChild("Backpack"), plr.Character
            if (bp and bp:FindFirstChild("Gun")) or (char and char:FindFirstChild("Gun")) then return plr end
        end
    end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0 then
            local role = getPlayerRole(plr)
            if role == "Sheriff" or role == "Hero" then return plr end
        end
    end
    return nil
end

local function getGunHolder() return getSheriff() end
local function lpHasGun()
    local bp, char = LP:FindFirstChild("Backpack"), LP.Character
    return (bp and bp:FindFirstChild("Gun")) or (char and char:FindFirstChild("Gun"))
end

local function getTargetByUsername(query)
    if not query or query == "" then return nil end
    query = string.lower(query)
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP and (string.lower(plr.Name) == query or string.lower(plr.DisplayName) == query) then return plr end
    end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LP then
            if string.lower(plr.Name):sub(1, #query) == query or string.lower(plr.DisplayName):sub(1, #query) == query then return plr end
        end
    end
    return nil
end

local function isInList(list, name)
    for _, n in ipairs(list) do if string.lower(n) == string.lower(name) then return true end end
    return false
end
local function addToList(list, name) if isInList(list, name) then return false end table.insert(list, name); return true end
local function removeFromList(list, name)
    for i, n in ipairs(list) do
        if string.lower(n) == string.lower(name) then table.remove(list, i); return true end
    end
    return false
end
local function isWhitelisted(plr) if not plr then return false end return isInList(settingsConfig.whitelist, plr.Name) end
local function getFirstCustomTarget()
    for _, name in ipairs(settingsConfig.flingList) do
        local plr = getTargetByUsername(name)
        if plr and targetIsAlive(plr) then return plr end
    end
    return nil
end

local function triggerMenuReset()
    pcall(function()
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Escape, false, game)
        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Escape, false, game)
        task.wait(0.15)
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.R, false, game)
        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.R, false, game)
        task.wait(0.15)
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Return, false, game)
        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Return, false, game)
    end)
end

local blackScreen = nil
local function alive()
    local c = LP.Character
    return c and c:FindFirstChild("Humanoid") and c.Humanoid.Health > 0 and c:FindFirstChild("HumanoidRootPart")
end

local function standUp()
    local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if hum then hum.PlatformStand = false; hum.AutoRotate = true; hum:ChangeState(Enum.HumanoidStateType.GettingUp) end
    if root then root.Anchored = false; root.AssemblyLinearVelocity = Vector3.zero; root.AssemblyAngularVelocity = Vector3.zero end
end

local function hideSky()
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.PlatformStand = true end
    if root then root.Anchored = true; root.CFrame = HIDE_POS; root.AssemblyLinearVelocity = Vector3.zero; root.AssemblyAngularVelocity = Vector3.zero end
end

local function cancelFarmTween()
    if currentFarmTween then pcall(function() currentFarmTween:Cancel() end) currentFarmTween = nil end
    if farmVelocityConn then farmVelocityConn:Disconnect() farmVelocityConn = nil end
    currentTargetCoin = nil
end

applyLowDeviceOptimizations = function(enabled)
    pcall(function()
        if enabled then
            RunService:Set3dRenderingEnabled(false)
            if blackScreen then blackScreen.Visible = true end
            UserGameSettings.SavedQualityLevel = Enum.SavedQualityLevel.Level0
            Lighting.GlobalShadows = false
            Lighting.FogEnd = 9e9
            for _, v in ipairs(Lighting:GetChildren()) do
                if v:IsA("PostEffect") or v:IsA("Sky") or v:IsA("Atmosphere") then v.Enabled = false end
            end
            collectgarbage("collect")
        else
            RunService:Set3dRenderingEnabled(true)
            if blackScreen then blackScreen.Visible = false end
            UserGameSettings.SavedQualityLevel = Enum.SavedQualityLevel.Level10
            Lighting.GlobalShadows = true
            for _, v in ipairs(Lighting:GetChildren()) do
                if v:IsA("PostEffect") or v:IsA("Sky") or v:IsA("Atmosphere") then v.Enabled = true end
            end
        end
    end)
end

local fpsBoostMaid = nil
applyFpsBoost = function(enabled)
    pcall(function()
        if enabled then
            Lighting.GlobalShadows = false
            Lighting.FogEnd = 9e9
            for _, v in ipairs(Lighting:GetChildren()) do
                if v:IsA("PostEffect") or v:IsA("Sky") or v:IsA("Atmosphere") then v.Enabled = false end
            end
            pcall(function()
                Workspace.Terrain.WaterWaveSize = 0
                Workspace.Terrain.WaterTransparency = 0
                Workspace.Terrain.WaterReflectance = 0
                Workspace.Terrain.Decoration = false
            end)
            local function potatoPart(v)
                if v:IsA("BasePart") then v.CastShadow = false; v.Material = Enum.Material.SmoothPlastic; v.Reflectance = 0
                elseif v:IsA("Decal") or v:IsA("Texture") then v.Transparency = 1
                elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") or v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") then v.Enabled = false
                elseif v:IsA("SpecialMesh") then v.TextureId = ""
                elseif v:IsA("MeshPart") then v.TextureID = ""; v.Material = Enum.Material.SmoothPlastic; v.CastShadow = false end
            end
            for _, v in ipairs(Workspace:GetDescendants()) do potatoPart(v) end
            if not fpsBoostMaid then
                fpsBoostMaid = Workspace.DescendantAdded:Connect(function(v) if state.fpsBoost then potatoPart(v) end end)
            end
            collectgarbage("collect")
        else
            Lighting.GlobalShadows = true
            for _, v in ipairs(Lighting:GetChildren()) do
                if v:IsA("PostEffect") or v:IsA("Sky") or v:IsA("Atmosphere") then v.Enabled = true end
            end
            if fpsBoostMaid then fpsBoostMaid:Disconnect() fpsBoostMaid = nil end
            pcall(function() Workspace.Terrain.Decoration = true end)
            for _, v in ipairs(Workspace:GetDescendants()) do
                if v:IsA("BasePart") then v.CastShadow = true
                elseif v:IsA("Decal") or v:IsA("Texture") then v.Transparency = 0 end
            end
        end
    end)
end

local httpRequest = request or http_request or (syn and syn.request) or (fluxus and fluxus.request)
local function sendAppleWareWebhook(title, description, fields, color)
    if settingsConfig.webhookUrl == "" then return end
    local ping = settingsConfig.discordUserId ~= "" and ("<@" .. settingsConfig.discordUserId .. ">") or ""
    local embed = {title = title, description = description, color = color or 0x2B2D31, fields = fields or {},
        footer = { text = "Appleware • " .. os.date("%H:%M:%S") }, timestamp = DateTime.now():ToIsoDate()}
    local data = {content = ping ~= "" and (ping .. " Status update:") or nil, username = "Appleware", embeds = {embed},
        allowed_mentions = { parse = {"users"} }}
    pcall(function()
        if httpRequest then
            httpRequest({Url = settingsConfig.webhookUrl, Method = "POST",
                Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(data)})
        end
    end)
end

local function sendStatusWebhook()
    local elapsed = tick() - sessionStartTime
    local hours = math.floor(elapsed / 3600)
    local minutes = math.floor((elapsed % 3600) / 60)
    local seconds = math.floor(elapsed % 60)
    local elapsedHours = math.max(elapsed / 3600, 0.001)
    local rate = math.floor((totalCoinsEarned or 0) / elapsedHours)
    local timeFormatted = hours == 0 and string.format("%dm %ds", minutes, seconds) or string.format("%dh %dm %ds", hours, minutes, seconds)
    sendAppleWareWebhook("Session Progress Report", "Update for " .. tostring(LP.Name), {
        {name = "User", value = "`" .. tostring(LP.Name) .. "`\nID: `" .. tostring(LP.UserId) .. "`", inline = true},
        {name = "Coins", value = "Bag: **" .. tostring(currentCoinCount) .. "/" .. tostring(maxCoinCount) .. "**\nTotal: **" .. tostring(totalCoinsEarned) .. "**", inline = true},
        {name = "Performance", value = "Time: **" .. timeFormatted .. "**\nRate: **" .. tostring(rate) .. " coins/hr**", inline = false},
        {name = "Server", value = "Place: `" .. tostring(game.PlaceId) .. "`\nJob: `" .. tostring(game.JobId) .. "`", inline = false}
    }, 0xFFFFFF)
end

task.spawn(function()
    while true do
        local waitTime = settingsConfig.webhookCooldown
        if not waitTime or waitTime < 5 then waitTime = 30 end
        task.wait(waitTime)
        if state.sendOnFull and settingsConfig.webhookUrl ~= "" then sendStatusWebhook() end
    end
end)

local function findTool(name)
    local char, bp = LP.Character, LP:FindFirstChild("Backpack")
    if char and char:FindFirstChild(name) then return char[name] end
    if bp and bp:FindFirstChild(name) then return bp[name] end
    return nil
end

local function equipTool(name)
    local char = LP.Character
    local bp = LP:FindFirstChild("Backpack")
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if not char then return nil end
    local existing = char:FindFirstChild(name)
    if existing then return existing end
    local tool = bp and bp:FindFirstChild(name)
    if tool and hum then
        pcall(function() hum:EquipTool(tool) end)
        task.wait(0.15)
        if not char:FindFirstChild(name) and tool.Parent == bp then pcall(function() tool.Parent = char end) end
        task.wait(0.05)
        return char:FindFirstChild(name)
    end
    return nil
end

local function executeKillBehind(targetPlayer)
    local targetChar = targetPlayer.Character
    local targetRoot = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
    local targetHumanoid = targetChar and targetChar:FindFirstChildOfClass("Humanoid")
    local myRoot = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if not (targetHumanoid and targetHumanoid.Health > 0 and targetRoot and myRoot) then return end
    local tStart = tick()
    while tick() - tStart < 2.0 and targetHumanoid.Health > 0 and targetRoot.Parent and myRoot.Parent and alive() do
        local knife = LP.Character and LP.Character:FindFirstChild("Knife")
        if not knife then knife = equipTool("Knife") end
        if knife then
            myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 0, 3.2)
            myRoot.AssemblyLinearVelocity = Vector3.zero
            myRoot.AssemblyAngularVelocity = Vector3.zero
            pcall(function() knife:Activate() end)
            pcall(function()
                for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
                    if v:IsA("RemoteEvent") then
                        local n = string.lower(v.Name)
                        if n:find("knife") or n:find("hit") or n:find("stab") or n:find("kill") or n:find("slash") then
                            pcall(function() v:FireServer(targetRoot.Position); v:FireServer(targetPlayer); v:FireServer(targetChar) end)
                        end
                    end
                end
            end)
        end
        task.wait(0.04)
    end
end

local function killCustomNow()
    task.spawn(function()
        if #settingsConfig.flingList == 0 then setStatus("Custom Targets list is empty!") return end
        local wasFarming, wasXpFarming = state.farm, state.xpFarm
        state.farm, state.xpFarm = false, false
        cancelFarmTween(); isExecutingAction = true
        local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        local savedCF = root and root.CFrame or HIDE_POS
        standUp(); task.wait(0.15)
        local knife = findTool("Knife") or equipTool("Knife")
        if not knife then
            for i = 1, 5 do task.wait(0.1); knife = equipTool("Knife"); if knife then break end end
        end
        if not knife then
            setStatus("No Knife found! You must be Murderer.")
            isExecutingAction = false; state.farm, state.xpFarm = wasFarming, wasXpFarming; return
        end
        setStatus("Killing " .. #settingsConfig.flingList .. " custom target(s)...")
        for _, name in ipairs(settingsConfig.flingList) do
            local plr = getTargetByUsername(name)
            if plr and targetIsAlive(plr) then executeKillBehind(plr) end
        end
        setStatus("Finished custom kills"); task.wait(0.25)
        local currRoot = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if currRoot then currRoot.Anchored = false; currRoot.CFrame = savedCF; currRoot.AssemblyLinearVelocity = Vector3.zero end
        if hum then hum.PlatformStand = false; hum:ChangeState(Enum.HumanoidStateType.GettingUp) end
        isExecutingAction = false; state.farm, state.xpFarm = wasFarming, wasXpFarming
    end)
end

local function flingCustomNow()
    task.spawn(function()
        if #settingsConfig.flingList == 0 then setStatus("Custom Targets list is empty!") return end
        local wasFarming, wasXpFarming = state.farm, state.xpFarm
        state.farm, state.xpFarm = false, false; cancelFarmTween()
        setStatus("Flinging " .. #settingsConfig.flingList .. " target(s)...")
        for _, name in ipairs(settingsConfig.flingList) do
            local plr = getTargetByUsername(name)
            if plr and targetIsAlive(plr) and not isWhitelisted(plr) then
                task.spawn(function() reliableFling(plr) end); task.wait(0.1)
            end
        end
        task.wait(0.15); state.farm, state.xpFarm = wasFarming, wasXpFarming
    end)
end

local function flingAllNow()
    task.spawn(function()
        local wasFarming, wasXpFarming = state.farm, state.xpFarm
        state.farm, state.xpFarm = false, false; cancelFarmTween()
        setStatus("Flinging all players (skipping whitelist)...")
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= LP and not isWhitelisted(plr) then
                local tHum = plr.Character and plr.Character:FindFirstChildOfClass("Humanoid")
                if tHum and tHum.Health > 0 then task.spawn(function() reliableFling(plr) end); task.wait(0.1) end
            end
        end
        state.farm, state.xpFarm = wasFarming, wasXpFarming
    end)
end

local function flingMurdererNow()
    task.spawn(function()
        local wasFarming, wasXpFarming = state.farm, state.xpFarm
        state.farm, state.xpFarm = false, false; cancelFarmTween()
        local mur = getMurd()
        if mur and mur.Character and not isWhitelisted(mur) then
            setStatus("Flinging Murderer..."); task.spawn(function() reliableFling(mur) end)
        elseif mur and isWhitelisted(mur) then setStatus("Murderer is whitelisted!")
        else setStatus("Murderer not found!") end
        state.farm, state.xpFarm = wasFarming, wasXpFarming
    end)
end

local function flingSheriffNow()
    task.spawn(function()
        local wasFarming, wasXpFarming = state.farm, state.xpFarm
        state.farm, state.xpFarm = false, false; cancelFarmTween()
        local sheriff = getSheriff()
        if sheriff and sheriff.Character and not isWhitelisted(sheriff) then
            setStatus("Flinging Sheriff..."); task.spawn(function() reliableFling(sheriff) end)
        elseif sheriff and isWhitelisted(sheriff) then setStatus("Sheriff is whitelisted!")
        else setStatus("Sheriff not found!") end
        state.farm, state.xpFarm = wasFarming, wasXpFarming
    end)
end

local function grabGunNow()
    task.spawn(function()
        if alive() then
            local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
            if root then
                local found = false
                for _, v in pairs(Workspace:GetDescendants()) do
                    if v.Name == "GunDrop" and v:IsA("BasePart") then
                        found = true
                        pcall(function() firetouchinterest(root, v, 0); firetouchinterest(root, v, 1) end)
                        setStatus("Grabbed gun drop!"); break
                    end
                end
                if not found then setStatus("No dropped gun found!") end
            end
        end
    end)
end

local function getRole() return getPlayerRole(LP) end

local function autoKillAllPlayers()
    standUp(); task.wait(0.1)
    local knife = findTool("Knife")
    if not knife then
        for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
            if v:IsA("Tool") and v.Name == "Knife" then knife = v:Clone(); knife.Parent = LP.Character; break end
        end
    end
    knife = knife or equipTool("Knife")
    if not knife then
        for i = 1, 10 do task.wait(0.1); knife = equipTool("Knife"); if knife then break end end
    end
    if not knife then setStatus("No knife found to stab players!") return end
    setStatus("Stabbing players from behind...")
    for _, targetPlayer in ipairs(Players:GetPlayers()) do
        if targetPlayer ~= LP then
            local tHum = targetPlayer.Character and targetPlayer.Character:FindFirstChildOfClass("Humanoid")
            if tHum and tHum.Health > 0 then executeKillBehind(targetPlayer) end
        end
    end
end

local function killAllNow()
    task.spawn(function()
        local wasFarming, wasXpFarming = state.farm, state.xpFarm
        state.farm, state.xpFarm = false, false; cancelFarmTween()
        local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        local savedCF = root and root.CFrame or HIDE_POS
        standUp(); task.wait(0.1)
        local knife = findTool("Knife") or equipTool("Knife")
        if not knife then setStatus("No knife found! You need to be Murderer.")
        else
            setStatus("Killing all players...")
            for _, targetPlayer in ipairs(Players:GetPlayers()) do
                if targetPlayer ~= LP then
                    local tHum = targetPlayer.Character and targetPlayer.Character:FindFirstChildOfClass("Humanoid")
                    if tHum and tHum.Health > 0 then executeKillBehind(targetPlayer) end
                end
            end
            setStatus("Kill all completed!")
        end
        task.wait(0.2)
        local currRoot = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if currRoot then currRoot.Anchored = false currRoot.CFrame = savedCF end
        if hum then hum.PlatformStand = false end
        state.farm, state.xpFarm = wasFarming, wasXpFarming
    end)
end

local function killSheriffNow()
    task.spawn(function()
        local wasFarming, wasXpFarming = state.farm, state.xpFarm
        state.farm, state.xpFarm = false, false; cancelFarmTween()
        local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        local savedCF = root and root.CFrame or HIDE_POS
        standUp(); task.wait(0.1)
        local knife = findTool("Knife") or equipTool("Knife")
        if not knife then setStatus("No knife found! You need to be Murderer.")
        else
            local sheriff = getSheriff()
            if sheriff and sheriff.Character then
                setStatus("Killing Sheriff..."); executeKillBehind(sheriff); setStatus("Sheriff eliminated!")
            else setStatus("Sheriff not found!") end
        end
        task.wait(0.2)
        local currRoot = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if currRoot then currRoot.Anchored = false currRoot.CFrame = savedCF end
        if hum then hum.PlatformStand = false end
        state.farm, state.xpFarm = wasFarming, wasXpFarming
    end)
end

local function teleportToLobby()
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if root then root.CFrame = LOBBY_CFRAME; setStatus("Teleported to Lobby")
    else setStatus("No character found") end
end

local function teleportToMap()
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if not root then setStatus("No character found") return end
    local mapPositions = {
        ["milbase"] = Vector3.new(-81.64, 208.01, 9100.18),
        ["police"] = Vector3.new(9.00, 289.28, 8952.20),
        ["bio"] = Vector3.new(607.35, 297.50, 9823.72),
        ["house"] = Vector3.new(36.42, 269.53, 8953.00),
        ["workplace"] = Vector3.new(65.30, 291.81, 9067.61),
        ["mansion"] = Vector3.new(-56.66, 272.40, 8957.84),
        ["factory"] = Vector3.new(-3.55, 274.13, 8892.21),
        ["research"] = Vector3.new(9.24, 292.13, 8984.99),
        ["bank"] = Vector3.new(37.84, 279.46, 9023.33),
    }
    for _, map in ipairs(Workspace:GetChildren()) do
        local coinContainer = map:FindFirstChild("CoinContainer") or (map.Name == "CoinContainer" and map)
        if coinContainer then
            local mapName = string.lower(map.Name):gsub("%s+", "")
            for key, pos in pairs(mapPositions) do
                if string.find(mapName, key) then root.CFrame = CFrame.new(pos); setStatus("Teleported to " .. map.Name); return end
            end
            local anyPart = map:FindFirstChildWhichIsA("BasePart", true)
            if anyPart then root.CFrame = CFrame.new(anyPart.Position + Vector3.new(0, 6, 0)); setStatus("Teleported to Map (fallback)"); return end
        end
    end
    setStatus("No map found / not in round")
end

local function teleportToPlayer(plr, label)
    if not plr then setStatus((label or "Target") .. " not found!") return end
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    local tRoot = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not root then setStatus("No character found") return end
    if not tRoot then setStatus(plr.Name .. " has no character") return end
    root.CFrame = tRoot.CFrame * CFrame.new(0, 0, 3)
    setStatus("Teleported to " .. plr.Name)
end

local function teleportToMurderer() teleportToPlayer(getMurd(), "Murderer") end
local function teleportToSheriff() teleportToPlayer(getSheriff(), "Sheriff") end
local function teleportToCustom()
    local target = getFirstCustomTarget()
    if not target then setStatus("No alive players in Custom Targets list!") return end
    teleportToPlayer(target, "Custom")
end

task.spawn(function()
    while true do
        task.wait(0.5)
        if not alive() then continue end
        if state.loopFlingAll or state.loopFlingMur or state.loopFlingSheriff or state.loopFlingCustom then
            isExecutingAction = true; cancelFarmTween()
            if state.loopFlingAll then
                for _, plr in ipairs(Players:GetPlayers()) do
                    if plr ~= LP and state.loopFlingAll and alive() and not isWhitelisted(plr) then
                        task.spawn(function() reliableFling(plr) end); task.wait(0.15)
                    end
                end
            elseif state.loopFlingMur then
                local mur = getMurd()
                if mur and alive() and not isWhitelisted(mur) then task.spawn(function() reliableFling(mur) end) end
            elseif state.loopFlingSheriff then
                if lpHasGun() then state.loopFlingSheriff = false; updateAllToggleVisuals("loopFlingSheriff"); setStatus("Got the gun! Sheriff loop fling disabled.")
                else
                    local sher = getSheriff()
                    if sher and alive() and not isWhitelisted(sher) then task.spawn(function() reliableFling(sher) end) end
                end
            elseif state.loopFlingCustom then
                for _, name in ipairs(settingsConfig.flingList) do
                    local t = getTargetByUsername(name)
                    if t and alive() and not isWhitelisted(t) then task.spawn(function() reliableFling(t) end) end
                end
            end
            isExecutingAction = false; task.wait(1)
        end
    end
end)

local Spots = { [1] = Vector3.new(25, 508, 50), [2] = Vector3.new(13, 508, 52), [3] = Vector3.new(1, 508, 49) }
local savedVotingCFrame = nil
local votingRunning = false

local function getMapNames()
    local mapNames = {"Slot 1: N/A", "Slot 2: N/A", "Slot 3: N/A"}
    local boards = {}
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("SurfaceGui") then
            local part = obj.Adornee or obj.Parent
            if part and part:IsA("BasePart") then
                local isVotingBoard, potentialName = false, nil
                for _, label in ipairs(obj:GetDescendants()) do
                    if label:IsA("TextLabel") and label.Visible then
                        local text, lowerText = label.Text, string.lower(label.Text)
                        if string.match(lowerText, "votes:") then isVotingBoard = true
                        elseif text ~= "" and lowerText ~= "map name" and lowerText ~= "label" and lowerText ~= "0" and lowerText ~= "???" then potentialName = text end
                    end
                end
                if isVotingBoard and potentialName then table.insert(boards, { Name = potentialName, Pos = part.Position }) end
            end
        end
    end
    for i, spotPos in ipairs(Spots) do
        local closestDist, closestName = 45, "N/A"
        for _, board in ipairs(boards) do
            local dist = (board.Pos - spotPos).Magnitude
            if dist < closestDist then closestDist = dist; closestName = board.Name end
        end
        mapNames[i] = "Slot " .. i .. ": " .. closestName
    end
    return mapNames
end

local function tpToSpot(index)
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if root then savedVotingCFrame = root.CFrame; root.CFrame = CFrame.new(Spots[index]); setStatus("Teleported to Spot " .. tostring(index)) end
end

local function resetX4(index)
    if votingRunning then setStatus("Voting sequence already running!") return end
    votingRunning = true; isExecutingAction = true; cancelFarmTween()
    task.spawn(function()
        pcall(function()
            local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
            if root then savedVotingCFrame = root.CFrame end
            setStatus("Spot " .. tostring(index) .. " - Starting...")
            for i = 1, 4 do
                if not votingRunning then break end
                local char = LP.Character
                local rt = char and char:FindFirstChild("HumanoidRootPart")
                if rt then rt.CFrame = CFrame.new(Spots[index]) end
                task.wait(0.4)
                local hum = char and char:FindFirstChildOfClass("Humanoid")
                if hum then hum.Health = 0 end
                setStatus(string.format("Spot %d - Reset %d/4", index, i))
                local finished = false
                local conn
                conn = LP.CharacterAdded:Connect(function() finished = true; if conn then conn:Disconnect() end end)
                local timer = 0
                while not finished and timer < 15 and votingRunning do task.wait(0.1); timer = timer + 0.1 end
                if conn then conn:Disconnect() end
                task.wait(0.5)
            end
            local char = LP.Character
            local rt = char and char:FindFirstChild("HumanoidRootPart")
            if rt and savedVotingCFrame then rt.CFrame = savedVotingCFrame end
            setStatus("Spot " .. tostring(index) .. " - Finished")
        end)
        votingRunning = false; isExecutingAction = false
    end)
end

-- ==================== UI ====================
buildUI = function(rebuilding)
    mainToggleButtons = {}
    quickToggleButtons = {}

    local parent = getGuiParent()
    pcall(function()
        local old = parent:FindFirstChild("Appleware")
        if old then old:Destroy() end
    end)

    local BG = Color3.fromRGB(6, 6, 6)
    local SIDEBAR = Color3.fromRGB(10, 10, 10)
    local CARD = Color3.fromRGB(14, 14, 14)
    local CARD_HOVER = Color3.fromRGB(22, 22, 22)
    local BORDER = Color3.fromRGB(28, 28, 28)
    local TEXT = Color3.fromRGB(245, 245, 245)
    local TEXT_DIM = Color3.fromRGB(140, 140, 140)
    local ACCENT = Color3.fromRGB(255, 255, 255)
    local WHITE_HEX = Color3.fromHex("#FFFFFF")
    local SECRET_BLACK = Color3.fromRGB(0, 0, 0)

    local baseWidth = 540 * selectedScale
    local baseHeight = 420 * selectedScale
    local sidebarW = 138 * selectedScale
    local fontSizeMult = selectedScale
    local r = function(n) return UDim.new(0, n * selectedScale) end

    local sg = Instance.new("ScreenGui")
    sg.Name = "Appleware"
    sg.Parent = parent
    sg.ResetOnSpawn = false
    sg.DisplayOrder = 999999
    sg.IgnoreGuiInset = true

    blackScreen = Instance.new("Frame")
    blackScreen.Size = UDim2.new(1, 0, 1, 0)
    blackScreen.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    blackScreen.Visible = state.disable3d
    blackScreen.ZIndex = -1
    blackScreen.Parent = sg

    local main = Instance.new("Frame")
    main.Name = "Main"
    main.AnchorPoint = Vector2.new(0.5, 0.5)
    main.Position = UDim2.new(0.5, 0, 0.5, 0)
    main.Size = UDim2.new(0, 0, 0, 0)
    main.BackgroundColor3 = BG
    main.Active = true
    main.Draggable = true
    main.ClipsDescendants = true
    main.Parent = sg
    main.Visible = false
    Instance.new("UICorner", main).CornerRadius = r(14)
    local mainStroke = Instance.new("UIStroke", main)
    mainStroke.Color = BORDER; mainStroke.Thickness = 1; mainStroke.Transparency = 0.3

    local sidebar = Instance.new("Frame")
    sidebar.Size = UDim2.new(0, sidebarW, 1, 0)
    sidebar.BackgroundColor3 = SIDEBAR
    sidebar.Parent = main
    Instance.new("UICorner", sidebar).CornerRadius = r(14)

    local sideGrad = Instance.new("UIGradient")
    sideGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(15, 15, 15)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(7, 7, 7))
    })
    sideGrad.Rotation = 90; sideGrad.Parent = sidebar

    local sidebarFix = Instance.new("Frame")
    sidebarFix.Size = UDim2.new(0, 16 * selectedScale, 1, 0)
    sidebarFix.Position = UDim2.new(1, -16 * selectedScale, 0, 0)
    sidebarFix.BackgroundColor3 = SIDEBAR
    sidebarFix.Parent = sidebar

    local sideDivider = Instance.new("Frame")
    sideDivider.Size = UDim2.new(0, 1, 1, -24 * selectedScale)
    sideDivider.Position = UDim2.new(1, 0, 0, 12 * selectedScale)
    sideDivider.BackgroundColor3 = BORDER; sideDivider.BackgroundTransparency = 0.5
    sideDivider.Parent = sidebar

    local logo = Instance.new("TextLabel")
    logo.Size = UDim2.new(1, -12 * selectedScale, 0, 48 * selectedScale)
    logo.Position = UDim2.new(0, 6 * selectedScale, 0, 4 * selectedScale)
    logo.BackgroundTransparency = 1
    logo.Text = "Appleware"; logo.TextColor3 = TEXT
    logo.Font = Enum.Font.GothamBold; logo.TextSize = 16 * fontSizeMult
    logo.TextXAlignment = Enum.TextXAlignment.Left; logo.Parent = sidebar

    local logoGlow = Instance.new("Frame")
    logoGlow.Size = UDim2.new(0, 4 * selectedScale, 0, 16 * selectedScale)
    logoGlow.Position = UDim2.new(0, 0, 0, 18 * selectedScale)
    logoGlow.BackgroundColor3 = ACCENT
    logoGlow.Parent = sidebar
    Instance.new("UICorner", logoGlow).CornerRadius = UDim.new(1, 0)

    task.spawn(function()
        while logoGlow.Parent do
            TweenService:Create(logoGlow, TweenInfo.new(1.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                Size = UDim2.new(0, 4 * selectedScale, 0, 26 * selectedScale),
                Position = UDim2.new(0, 0, 0, 13 * selectedScale)
            }):Play()
            task.wait(1.6)
            if not logoGlow.Parent then break end
            TweenService:Create(logoGlow, TweenInfo.new(1.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                Size = UDim2.new(0, 4 * selectedScale, 0, 16 * selectedScale),
                Position = UDim2.new(0, 0, 0, 18 * selectedScale)
            }):Play()
            task.wait(1.6)
        end
    end)

    local logoLine = Instance.new("Frame")
    logoLine.Size = UDim2.new(1, -24 * selectedScale, 0, 1)
    logoLine.Position = UDim2.new(0, 12 * selectedScale, 0, 50 * selectedScale)
    logoLine.BackgroundColor3 = BORDER; logoLine.BackgroundTransparency = 0.4
    logoLine.Parent = sidebar

    local tabList = Instance.new("Frame")
    tabList.Size = UDim2.new(1, -12 * selectedScale, 1, -68 * selectedScale)
    tabList.Position = UDim2.new(0, 6 * selectedScale, 0, 58 * selectedScale)
    tabList.BackgroundTransparency = 1; tabList.Parent = sidebar

    local tabLayout = Instance.new("UIListLayout")
    tabLayout.SortOrder = Enum.SortOrder.LayoutOrder
    tabLayout.Padding = UDim.new(0, 3 * selectedScale)
    tabLayout.Parent = tabList

    local slidingIndicator = Instance.new("Frame")
    slidingIndicator.Size = UDim2.new(0, 3 * selectedScale, 0, 18 * selectedScale)
    slidingIndicator.BackgroundColor3 = ACCENT
    slidingIndicator.ZIndex = 10
    slidingIndicator.Parent = sidebar
    Instance.new("UICorner", slidingIndicator).CornerRadius = UDim.new(1, 0)

    local content = Instance.new("Frame")
    content.Size = UDim2.new(1, -sidebarW - 20 * selectedScale, 1, -56 * selectedScale)
    content.Position = UDim2.new(0, sidebarW + 10 * selectedScale, 0, 10 * selectedScale)
    content.BackgroundTransparency = 1; content.ClipsDescendants = true
    content.Parent = main

    local tabs = {"Farm", "Combat", "Teleport", "ESP", "Webhook", "Misc", "UI", "Voting", "Secret"}
    local pages, pageGroups, btns = {}, {}, {}
    local currentTabName = "Farm"
    local switching = false

    local function makePage(name)
        local group = Instance.new("Frame")
        group.Name = name .. "_Group"
        group.Size = UDim2.new(1, 0, 1, 0)
        group.BackgroundTransparency = 1; group.Visible = false
        group.ClipsDescendants = true; group.Parent = content

        local pg = Instance.new("ScrollingFrame")
        pg.Name = "Page"
        pg.Size = UDim2.new(1, 0, 1, 0)
        pg.BackgroundTransparency = 1
        pg.ScrollBarThickness = 3 * selectedScale
        pg.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 60)
        pg.CanvasSize = UDim2.new(0, 0, 0, 0)
        pg.Parent = group

        local layout = Instance.new("UIListLayout")
        layout.SortOrder = Enum.SortOrder.LayoutOrder
        layout.Padding = UDim.new(0, 7 * selectedScale)
        layout.Parent = pg
        layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            pg.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 16 * selectedScale)
        end)

        local pad = Instance.new("UIPadding")
        pad.PaddingTop = UDim.new(0, 4); pad.PaddingBottom = UDim.new(0, 10); pad.PaddingRight = UDim.new(0, 8)
        pad.Parent = pg

        pages[name] = pg; pageGroups[name] = group
        return pg
    end

    local function moveSlidingIndicator(tabBtn)
        local posY = tabBtn.AbsolutePosition.Y - tabList.AbsolutePosition.Y + 6 * selectedScale
        TweenService:Create(slidingIndicator, TweenInfo.new(0.42, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Position = UDim2.new(0, 3 * selectedScale, 0, posY + 58 * selectedScale)
        }):Play()
    end

    local function switchTab(name)
        if switching or name == currentTabName then return end
        switching = true
        local oldName = currentTabName
        currentTabName = name

        for n, b in pairs(btns) do
            local isActive = (n == name)
            local isSecret = (n == "Secret")
            local targetColor
            if isSecret then
                targetColor = isActive and TEXT or SECRET_BLACK
            else
                targetColor = isActive and TEXT or TEXT_DIM
            end
            TweenService:Create(b, TweenInfo.new(0.28, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                BackgroundTransparency = isActive and 0 or 1,
                BackgroundColor3 = isActive and Color3.fromRGB(22, 22, 22) or Color3.fromRGB(0, 0, 0),
                TextColor3 = targetColor
            }):Play()
            for _, child in ipairs(b:GetChildren()) do
                if child:IsA("Frame") and child.Name == "Indicator" then child.Visible = isActive end
            end
        end

        if btns[name] then moveSlidingIndicator(btns[name]) end

        local oldGroup = pageGroups[oldName]
        local newGroup = pageGroups[name]
        local oldPage = pages[oldName]
        local newPage = pages[name]

        if oldGroup and oldPage then
            TweenService:Create(oldPage, TweenInfo.new(0.24, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
                Position = UDim2.new(-0.12, 0, 0, 0)
            }):Play()
            task.delay(0.24, function()
                if oldGroup and currentTabName ~= oldName then
                    oldGroup.Visible = false; oldPage.Position = UDim2.new(0, 0, 0, 0)
                end
            end)
        end

        if newGroup and newPage then
            newGroup.Visible = true
            newPage.Position = UDim2.new(0.12, 0, 0, 0)
            newPage.Size = UDim2.new(0.9, 0, 1, 0)
            TweenService:Create(newPage, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Position = UDim2.new(0, 0, 0, 0), Size = UDim2.new(1, 0, 1, 0)
            }):Play()
            task.spawn(function() task.wait(0.08); staggerFadeIn(newPage) end)
        end
        task.delay(0.4, function() switching = false end)
    end

    local firstTabBtn = nil

    for i, name in ipairs(tabs) do
        local isSecret = (name == "Secret")
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 32 * selectedScale)
        btn.BackgroundColor3 = (i == 1) and Color3.fromRGB(22, 22, 22) or Color3.fromRGB(0, 0, 0)
        btn.BackgroundTransparency = (i == 1) and 0 or 1
        btn.Text = "  " .. name
        if isSecret then
            btn.TextColor3 = SECRET_BLACK
        else
            btn.TextColor3 = (i == 1) and TEXT or TEXT_DIM
        end
        btn.Font = Enum.Font.GothamMedium
        btn.TextSize = 12.5 * fontSizeMult
        btn.TextXAlignment = Enum.TextXAlignment.Left
        btn.AutoButtonColor = false
        btn.Parent = tabList
        Instance.new("UICorner", btn).CornerRadius = r(8)
        btns[name] = btn
        if i == 1 then firstTabBtn = btn end

        local indicator = Instance.new("Frame")
        indicator.Name = "Indicator"
        indicator.Size = UDim2.new(0, 3 * selectedScale, 0.55, 0)
        indicator.Position = UDim2.new(0, 0, 0.225, 0)
        indicator.BackgroundColor3 = ACCENT
        indicator.Visible = (i == 1); indicator.Parent = btn
        Instance.new("UICorner", indicator).CornerRadius = UDim.new(1, 0)

        if not isSecret then
            btn.MouseEnter:Connect(function()
                if currentTabName ~= name then
                    TweenService:Create(btn, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                        BackgroundTransparency = 0.7,
                        BackgroundColor3 = Color3.fromRGB(18, 18, 18),
                        TextColor3 = Color3.fromRGB(200, 200, 200)
                    }):Play()
                end
            end)
            btn.MouseLeave:Connect(function()
                if currentTabName ~= name then
                    TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundTransparency = 1, TextColor3 = TEXT_DIM}):Play()
                end
            end)
        end

        attachRipple(btn, Color3.fromRGB(255, 255, 255))

        makePage(name)
        if i == 1 then pageGroups[name].Visible = true end

        btn.MouseButton1Click:Connect(function() switchTab(name) end)
    end

    task.defer(function() task.wait(0.3); if firstTabBtn then moveSlidingIndicator(firstTabBtn) end end)

    st = Instance.new("TextLabel")
    st.Size = UDim2.new(1, -sidebarW - 20 * selectedScale, 0, 30 * selectedScale)
    st.Position = UDim2.new(0, sidebarW + 10 * selectedScale, 1, -40 * selectedScale)
    st.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
    st.Text = "  Ready"; st.TextColor3 = TEXT
    st.Font = Enum.Font.GothamMedium; st.TextSize = 11.5 * fontSizeMult
    st.TextXAlignment = Enum.TextXAlignment.Left
    st.TextTruncate = Enum.TextTruncate.AtEnd
    st.Parent = main
    Instance.new("UICorner", st).CornerRadius = r(8)
    local stStroke = Instance.new("UIStroke", st)
    stStroke.Color = BORDER; stStroke.Transparency = 0.5
    local stAccent = Instance.new("Frame")
    stAccent.Size = UDim2.new(0, 3 * selectedScale, 0.55, 0)
    stAccent.Position = UDim2.new(0, 0, 0.225, 0)
    stAccent.BackgroundColor3 = ACCENT
    stAccent.Parent = st
    Instance.new("UICorner", stAccent).CornerRadius = UDim.new(1, 0)
    task.spawn(function()
        while stAccent.Parent do
            TweenService:Create(stAccent, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {BackgroundTransparency = 0.4}):Play()
            task.wait(1.5)
            if not stAccent.Parent then break end
            TweenService:Create(stAccent, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {BackgroundTransparency = 0}):Play()
            task.wait(1.5)
        end
    end)

    local function tgg(page, name, order, key)
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, 0, 0, 40 * selectedScale)
        row.LayoutOrder = order
        row.BackgroundColor3 = CARD
        row.Parent = page
        Instance.new("UICorner", row).CornerRadius = r(10)
        local rowStroke = Instance.new("UIStroke", row)
        rowStroke.Color = BORDER; rowStroke.Transparency = 0.55

        local rowScale = Instance.new("UIScale", row); rowScale.Scale = 1

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, -64 * selectedScale, 1, 0)
        label.Position = UDim2.new(0, 14 * selectedScale, 0, 0)
        label.BackgroundTransparency = 1
        label.Text = name; label.TextColor3 = TEXT
        label.Font = Enum.Font.GothamMedium; label.TextSize = 12.5 * fontSizeMult
        label.TextXAlignment = Enum.TextXAlignment.Left; label.Parent = row

        local glow = Instance.new("Frame")
        glow.Size = UDim2.new(0, 48 * selectedScale, 0, 26 * selectedScale)
        glow.Position = UDim2.new(1, -56 * selectedScale, 0.5, -13 * selectedScale)
        glow.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        glow.BackgroundTransparency = state[key] and 0.78 or 1
        glow.Parent = row
        Instance.new("UICorner", glow).CornerRadius = UDim.new(1, 0)

        local pill = Instance.new("Frame")
        pill.Size = UDim2.new(0, 44 * selectedScale, 0, 24 * selectedScale)
        pill.Position = UDim2.new(1, -54 * selectedScale, 0.5, -12 * selectedScale)
        pill.BackgroundColor3 = state[key] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(32, 32, 32)
        pill.Parent = row
        Instance.new("UICorner", pill).CornerRadius = UDim.new(1, 0)

        local pillScale = Instance.new("UIScale", pill); pillScale.Scale = 1

        local pillInner = Instance.new("Frame")
        pillInner.Size = UDim2.new(1, -4 * selectedScale, 1, -4 * selectedScale)
        pillInner.Position = UDim2.new(0, 2 * selectedScale, 0, 2 * selectedScale)
        pillInner.BackgroundColor3 = state[key] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(22, 22, 22)
        pillInner.Parent = pill
        Instance.new("UICorner", pillInner).CornerRadius = UDim.new(1, 0)

        local dot = Instance.new("Frame")
        dot.Size = UDim2.new(0, 16 * selectedScale, 0, 16 * selectedScale)
        dot.Position = state[key] and UDim2.new(1, -20 * selectedScale, 0.5, -8 * selectedScale) or UDim2.new(0, 4 * selectedScale, 0.5, -8 * selectedScale)
        dot.BackgroundColor3 = state[key] and Color3.fromRGB(0, 0, 0) or Color3.fromRGB(200, 200, 200)
        dot.Parent = pill
        Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)

        mainToggleButtons[key] = {pill = pill, dot = dot, glow = glow, inner = pillInner, pillScale = pillScale}
        startPulsingGlow(glow, function() return state[key] end)

        local hit = Instance.new("TextButton")
        hit.Size = UDim2.new(1, 0, 1, 0)
        hit.BackgroundTransparency = 1; hit.Text = ""
        hit.AutoButtonColor = false; hit.Parent = row

        hit.MouseEnter:Connect(function()
            TweenService:Create(row, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {BackgroundColor3 = CARD_HOVER}):Play()
            TweenService:Create(rowStroke, TweenInfo.new(0.3), {Transparency = 0.12, Color = Color3.fromRGB(80, 80, 80)}):Play()
            TweenService:Create(rowScale, TweenInfo.new(0.32, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1.02}):Play()
            TweenService:Create(row, TweenInfo.new(0.32, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Rotation = 0.7}):Play()
        end)
        hit.MouseLeave:Connect(function()
            TweenService:Create(row, TweenInfo.new(0.28, Enum.EasingStyle.Quint), {BackgroundColor3 = CARD}):Play()
            TweenService:Create(rowStroke, TweenInfo.new(0.3), {Transparency = 0.55, Color = BORDER}):Play()
            TweenService:Create(rowScale, TweenInfo.new(0.42, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Scale = 1}):Play()
            TweenService:Create(row, TweenInfo.new(0.42, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Rotation = 0}):Play()
        end)
        hit.MouseButton1Down:Connect(function()
            TweenService:Create(rowScale, TweenInfo.new(0.08, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Scale = 0.97}):Play()
        end)
        hit.MouseButton1Up:Connect(function()
            TweenService:Create(rowScale, TweenInfo.new(0.5, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Scale = 1.02}):Play()
        end)
        attachRipple(hit, Color3.fromRGB(255, 255, 255))
        hit.MouseButton1Click:Connect(function()
            setToggleState(key, not state[key])
            if mainToggleButtons[key] then
                TweenService:Create(pillInner, TweenInfo.new(0.28, Enum.EasingStyle.Quint), {
                    BackgroundColor3 = state[key] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(22, 22, 22)
                }):Play()
            end
        end)
    end

    local function addActionButton(page, name, order, callback)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 38 * selectedScale)
        btn.LayoutOrder = order
        btn.BackgroundColor3 = CARD; btn.Text = ""
        btn.ClipsDescendants = true; btn.Parent = page
        Instance.new("UICorner", btn).CornerRadius = r(10)
        local btnStroke = Instance.new("UIStroke", btn)
        btnStroke.Color = BORDER; btnStroke.Transparency = 0.55

        local textLbl = Instance.new("TextLabel")
        textLbl.Size = UDim2.new(1, -12 * selectedScale, 1, 0)
        textLbl.Position = UDim2.new(0, 12 * selectedScale, 0, 0)
        textLbl.BackgroundTransparency = 1
        textLbl.Text = name; textLbl.TextColor3 = TEXT
        textLbl.Font = Enum.Font.GothamMedium; textLbl.TextSize = 12.5 * fontSizeMult
        textLbl.TextXAlignment = Enum.TextXAlignment.Left; textLbl.Parent = btn

        local accent = Instance.new("Frame")
        accent.Size = UDim2.new(0, 0, 0.5, 0)
        accent.Position = UDim2.new(0, 0, 0.25, 0)
        accent.BackgroundColor3 = ACCENT; accent.Parent = btn
        Instance.new("UICorner", accent).CornerRadius = UDim.new(1, 0)

        btn.MouseEnter:Connect(function()
            TweenService:Create(btn, TweenInfo.new(0.25), {BackgroundColor3 = CARD_HOVER}):Play()
            TweenService:Create(accent, TweenInfo.new(0.32, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 3 * selectedScale, 0.5, 0)}):Play()
            TweenService:Create(textLbl, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Position = UDim2.new(0, 18 * selectedScale, 0, 0)}):Play()
        end)
        btn.MouseLeave:Connect(function()
            TweenService:Create(btn, TweenInfo.new(0.28), {BackgroundColor3 = CARD}):Play()
            TweenService:Create(accent, TweenInfo.new(0.24), {Size = UDim2.new(0, 0, 0.5, 0)}):Play()
            TweenService:Create(textLbl, TweenInfo.new(0.35, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Position = UDim2.new(0, 12 * selectedScale, 0, 0)}):Play()
        end)

        attachClickBounce(btn); attachHoverLift(btn, 1.025); attachHoverTilt(btn, -0.4)
        attachShine(btn, 0.55)
        attachRipple(btn, Color3.fromRGB(255, 255, 255))
        btn.MouseButton1Click:Connect(callback)
    end

    local function addSection(page, text, order)
        local wrap = Instance.new("Frame")
        wrap.Size = UDim2.new(1, 0, 0, 22 * selectedScale)
        wrap.LayoutOrder = order
        wrap.BackgroundTransparency = 1; wrap.Parent = page
        local bar = Instance.new("Frame")
        bar.Size = UDim2.new(0, 2 * selectedScale, 0, 10 * selectedScale)
        bar.Position = UDim2.new(0, 0, 0.5, -5 * selectedScale)
        bar.BackgroundColor3 = ACCENT; bar.BackgroundTransparency = 0.25
        bar.Parent = wrap
        Instance.new("UICorner", bar).CornerRadius = UDim.new(1, 0)
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(1, -10 * selectedScale, 1, 0)
        lbl.Position = UDim2.new(0, 10 * selectedScale, 0, 0)
        lbl.BackgroundTransparency = 1
        lbl.Text = text; lbl.TextColor3 = TEXT_DIM
        lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 11 * fontSizeMult
        lbl.TextXAlignment = Enum.TextXAlignment.Left; lbl.Parent = wrap
    end

    local function makeStepper(parent, xOff, label)
        local b = Instance.new("TextButton")
        b.Size = UDim2.new(0, 30 * selectedScale, 0, 26 * selectedScale)
        b.Position = UDim2.new(1, xOff, 0.5, -13 * selectedScale)
        b.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
        b.Text = label; b.TextColor3 = TEXT
        b.Font = Enum.Font.GothamBold; b.TextSize = 14 * fontSizeMult
        b.ClipsDescendants = true; b.Parent = parent
        Instance.new("UICorner", b).CornerRadius = r(7)
        b.MouseEnter:Connect(function() TweenService:Create(b, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(50, 50, 50)}):Play() end)
        b.MouseLeave:Connect(function() TweenService:Create(b, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(28, 28, 28)}):Play() end)
        attachClickBounce(b)
        attachRipple(b, Color3.fromRGB(255, 255, 255))
        return b
    end

    local function makeSliderRow(page, label, order, initialValue, minVal, maxVal, stateKey)
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, 0, 0, 56 * selectedScale)
        row.LayoutOrder = order
        row.BackgroundColor3 = CARD
        row.Parent = page
        Instance.new("UICorner", row).CornerRadius = r(10)
        local rowStroke = Instance.new("UIStroke", row)
        rowStroke.Color = BORDER; rowStroke.Transparency = 0.55

        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(0.6, 0, 0, 24 * selectedScale)
        lbl.Position = UDim2.new(0, 14 * selectedScale, 0, 4 * selectedScale)
        lbl.BackgroundTransparency = 1
        lbl.Text = label; lbl.TextColor3 = TEXT
        lbl.Font = Enum.Font.GothamMedium; lbl.TextSize = 11 * fontSizeMult
        lbl.TextXAlignment = Enum.TextXAlignment.Left; lbl.Parent = row

        local inputBox = Instance.new("TextBox")
        inputBox.Size = UDim2.new(0, 50 * selectedScale, 0, 24 * selectedScale)
        inputBox.Position = UDim2.new(1, -64 * selectedScale, 0, 4 * selectedScale)
        inputBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
        inputBox.BackgroundTransparency = 0.5
        inputBox.Text = tostring(initialValue)
        inputBox.TextColor3 = Color3.fromRGB(150, 200, 255)
        inputBox.Font = Enum.Font.Gotham; inputBox.TextSize = 11 * fontSizeMult
        inputBox.ClearTextOnFocus = false; inputBox.Parent = row
        Instance.new("UICorner", inputBox).CornerRadius = r(6)
        local ibStroke = Instance.new("UIStroke", inputBox)
        ibStroke.Color = Color3.fromRGB(0, 120, 255)

        local track = Instance.new("Frame")
        track.Size = UDim2.new(1, -28 * selectedScale, 0, 8 * selectedScale)
        track.Position = UDim2.new(0, 14 * selectedScale, 1, -18 * selectedScale)
        track.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        track.Parent = row
        Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)

        local fill = Instance.new("Frame")
        fill.Size = UDim2.new((initialValue - minVal) / (maxVal - minVal), 0, 1, 0)
        fill.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
        fill.Parent = track
        Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
        attachAnimatedGradient(fill, Color3.fromRGB(0, 100, 255), Color3.fromRGB(120, 200, 255), 3)

        local knob = Instance.new("Frame")
        knob.Size = UDim2.new(0, 16 * selectedScale, 0, 16 * selectedScale)
        knob.Position = UDim2.new((initialValue - minVal) / (maxVal - minVal), 0, 0.5, -8 * selectedScale)
        knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        knob.Parent = track
        Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

        local dragging = false
        local function updateFromInput(input)
            local mouseX = input.Position.X
            local trackPos = track.AbsolutePosition.X
            local trackSize = track.AbsoluteSize.X
            if trackSize == 0 then return end
            local percentage = math.clamp((mouseX - trackPos) / trackSize, 0, 1)
            local value = math.floor(minVal + (maxVal - minVal) * percentage + 0.5)
            state[stateKey] = value
            inputBox.Text = tostring(value)
            fill.Size = UDim2.new(percentage, 0, 1, 0)
            knob.Position = UDim2.new(percentage, 0, 0.5, -8 * selectedScale)
            saveSettings()
        end
        local function updateFromValue(value)
            value = math.clamp(value, minVal, maxVal)
            state[stateKey] = value
            inputBox.Text = tostring(value)
            local percentage = (value - minVal) / (maxVal - minVal)
            fill.Size = UDim2.new(percentage, 0, 1, 0)
            knob.Position = UDim2.new(percentage, 0, 0.5, -8 * selectedScale)
            saveSettings()
        end
        track.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true; updateFromInput(input)
                TweenService:Create(knob, TweenInfo.new(0.15), {Size = UDim2.new(0, 22 * selectedScale, 0, 22 * selectedScale), BackgroundColor3 = Color3.fromRGB(150, 200, 255)}):Play()
            end
        end)
        UIS.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then updateFromInput(input) end
        end)
        UIS.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
                TweenService:Create(knob, TweenInfo.new(0.35, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Size = UDim2.new(0, 16 * selectedScale, 0, 16 * selectedScale), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
            end
        end)
        inputBox.FocusLost:Connect(function()
            local val = tonumber(inputBox.Text)
            if val then updateFromValue(val) else inputBox.Text = tostring(state[stateKey]) end
        end)
    end

    -- ==================== SECRET TAB ====================
    addSection(pages["Secret"], "Movement", 1)
    tgg(pages["Secret"], "Fly (WASD / Joystick)", 2, "flyEnabled")
    makeSliderRow(pages["Secret"], "Fly Speed", 3, state.flySpeed or 50, 10, 300, "flySpeed")
    tgg(pages["Secret"], "Infinite Jump", 4, "infJumpEnabled")

    addSection(pages["Secret"], "Physics", 5)
    tgg(pages["Secret"], "Noclip", 6, "noclip")
    tgg(pages["Secret"], "Freeze (Anchor)", 7, "freeze")

    addSection(pages["Secret"], "Attributes", 8)
    tgg(pages["Secret"], "Speed Changer", 9, "speedEnabled")
    makeSliderRow(pages["Secret"], "Walk Speed", 10, state.speedValue or 16, 16, 500, "speedValue")
    tgg(pages["Secret"], "Jump Power", 11, "jumpEnabled")
    makeSliderRow(pages["Secret"], "Jump Power", 12, state.jumpValue or 50, 50, 500, "jumpValue")

    addSection(pages["Secret"], "Invisibility", 13)
    tgg(pages["Secret"], "Invisible (Ghost Mode)", 14, "invisibleEnabled")
    local invisHint = Instance.new("TextLabel")
    invisHint.Size = UDim2.new(1, 0, 0, 44 * selectedScale)
    invisHint.LayoutOrder = 15
    invisHint.BackgroundTransparency = 1
    invisHint.Text = "Clones your body into a ghost you control. Server scripts see the ghost's position as yours. Movement replicates to others normally."
    invisHint.TextColor3 = TEXT_DIM
    invisHint.Font = Enum.Font.Gotham
    invisHint.TextSize = 10.5 * fontSizeMult
    invisHint.TextXAlignment = Enum.TextXAlignment.Left
    invisHint.TextWrapped = true
    invisHint.Parent = pages["Secret"]

    addSection(pages["Secret"], "Visuals", 16)
    tgg(pages["Secret"], "Fullbright", 17, "fullbright")
    tgg(pages["Secret"], "FOV Changer", 18, "fovEnabled")
    makeSliderRow(pages["Secret"], "Field of View", 19, state.fovValue or 70, 30, 120, "fovValue")
    tgg(pages["Secret"], "Camera Max Zoom", 20, "cameraZoomEnabled")
    makeSliderRow(pages["Secret"], "Max Zoom Distance", 21, state.cameraMaxZoom or 500, 100, 2000, "cameraMaxZoom")
    tgg(pages["Secret"], "Info HUD", 22, "infoHudEnabled")

    addSection(pages["Secret"], "Utility", 23)
    tgg(pages["Secret"], "Instant Interact", 24, "instantInteract")

    addSection(pages["Secret"], "Teleportation", 25)
    tgg(pages["Secret"], "Click / Tap Teleport", 26, "clickTP")
    local tpHint = Instance.new("TextLabel")
    tpHint.Size = UDim2.new(1, 0, 0, 30 * selectedScale)
    tpHint.LayoutOrder = 27
    tpHint.BackgroundTransparency = 1
    tpHint.Text = "PC: click anywhere | Mobile: tap anywhere. Disable before using the fly buttons."
    tpHint.TextColor3 = TEXT_DIM
    tpHint.Font = Enum.Font.Gotham
    tpHint.TextSize = 10.5 * fontSizeMult
    tpHint.TextXAlignment = Enum.TextXAlignment.Left
    tpHint.TextWrapped = true
    tpHint.Parent = pages["Secret"]

    addSection(pages["Secret"], "Saved Positions", 28)
    for i = 1, 3 do
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, 0, 0, 36 * selectedScale)
        row.LayoutOrder = 28 + i
        row.BackgroundColor3 = CARD
        row.Parent = pages["Secret"]
        Instance.new("UICorner", row).CornerRadius = r(10)
        local rs = Instance.new("UIStroke", row)
        rs.Color = BORDER; rs.Transparency = 0.55

        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(0.5, 0, 1, 0)
        lbl.Position = UDim2.new(0, 14 * selectedScale, 0, 0)
        lbl.BackgroundTransparency = 1
        lbl.Text = "Slot " .. i
        lbl.TextColor3 = TEXT
        lbl.Font = Enum.Font.GothamMedium
        lbl.TextSize = 12 * fontSizeMult
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = row

        local saveBtn = Instance.new("TextButton")
        saveBtn.Size = UDim2.new(0, 60 * selectedScale, 0, 26 * selectedScale)
        saveBtn.Position = UDim2.new(1, -132 * selectedScale, 0.5, -13 * selectedScale)
        saveBtn.BackgroundColor3 = Color3.fromRGB(35, 140, 65)
        saveBtn.Text = "Save"
        saveBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        saveBtn.Font = Enum.Font.GothamBold
        saveBtn.TextSize = 10.5 * fontSizeMult
        saveBtn.ClipsDescendants = true
        saveBtn.Parent = row
        Instance.new("UICorner", saveBtn).CornerRadius = r(6)
        attachClickBounce(saveBtn); attachRipple(saveBtn)
        saveBtn.MouseButton1Click:Connect(function() saveCurrentPosition(i) end)

        local tpBtn = Instance.new("TextButton")
        tpBtn.Size = UDim2.new(0, 60 * selectedScale, 0, 26 * selectedScale)
        tpBtn.Position = UDim2.new(1, -68 * selectedScale, 0.5, -13 * selectedScale)
        tpBtn.BackgroundColor3 = Color3.fromRGB(35, 100, 200)
        tpBtn.Text = "Teleport"
        tpBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        tpBtn.Font = Enum.Font.GothamBold
        tpBtn.TextSize = 10.5 * fontSizeMult
        tpBtn.ClipsDescendants = true
        tpBtn.Parent = row
        Instance.new("UICorner", tpBtn).CornerRadius = r(6)
        attachClickBounce(tpBtn); attachRipple(tpBtn)
        tpBtn.MouseButton1Click:Connect(function() loadPosition(i) end)
    end

    addSection(pages["Secret"], "Quick Actions", 32)
    addActionButton(pages["Secret"], "Reset Character", 33, function()
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = 0 end
    end)

    -- ==================== FARM ====================
    tgg(pages["Farm"], "Farm (Safe)", 1, "farm")
    tgg(pages["Farm"], "XP Farm", 2, "xpFarm")
    tgg(pages["Farm"], "Disable 3D Rendering", 3, "disable3d")
    tgg(pages["Farm"], "FPS Boost (Potato)", 4, "fpsBoost")

    local speedRow = Instance.new("Frame")
    speedRow.Size = UDim2.new(1, 0, 0, 40 * selectedScale)
    speedRow.LayoutOrder = 5
    speedRow.BackgroundColor3 = CARD
    speedRow.Parent = pages["Farm"]
    Instance.new("UICorner", speedRow).CornerRadius = r(10)
    local speedStroke = Instance.new("UIStroke", speedRow)
    speedStroke.Color = BORDER; speedStroke.Transparency = 0.55

    local speedLbl = Instance.new("TextLabel")
    speedLbl.Size = UDim2.new(0.5, 0, 1, 0)
    speedLbl.Position = UDim2.new(0, 14 * selectedScale, 0, 0)
    speedLbl.BackgroundTransparency = 1
    speedLbl.Text = "Tween Speed"; speedLbl.TextColor3 = TEXT
    speedLbl.Font = Enum.Font.GothamMedium; speedLbl.TextSize = 12.5 * fontSizeMult
    speedLbl.TextXAlignment = Enum.TextXAlignment.Left; speedLbl.Parent = speedRow

    local speedMinus = makeStepper(speedRow, -100 * selectedScale, "-")
    local speedVal = Instance.new("TextLabel")
    speedVal.Size = UDim2.new(0, 34 * selectedScale, 0, 26 * selectedScale)
    speedVal.Position = UDim2.new(1, -66 * selectedScale, 0.5, -13 * selectedScale)
    speedVal.BackgroundTransparency = 1
    speedVal.Text = tostring(math.floor(TWEEN_SPEED))
    speedVal.TextColor3 = TEXT
    speedVal.Font = Enum.Font.GothamBold; speedVal.TextSize = 12.5 * fontSizeMult
    speedVal.Parent = speedRow
    local speedPlus = makeStepper(speedRow, -30 * selectedScale, "+")

    speedMinus.MouseButton1Click:Connect(function()
        if TWEEN_SPEED > 1 then
            TWEEN_SPEED = math.max(1, math.floor(TWEEN_SPEED) - 1)
            settingsConfig.tweenSpeed = TWEEN_SPEED; saveSettings()
            speedVal.Text = tostring(math.floor(TWEEN_SPEED))
        end
    end)
    speedPlus.MouseButton1Click:Connect(function()
        if TWEEN_SPEED < 100 then
            TWEEN_SPEED = math.min(100, math.floor(TWEEN_SPEED) + 1)
            settingsConfig.tweenSpeed = TWEEN_SPEED; saveSettings()
            speedVal.Text = tostring(math.floor(TWEEN_SPEED))
        end
    end)

    -- COMBAT
    tgg(pages["Combat"], "Auto Shoot Murderer", 1, "autoShootMur")
    tgg(pages["Combat"], "Auto Kill All", 2, "autoKillAll")
    tgg(pages["Combat"], "Auto Fling Murderer", 3, "autoFlingMur")
    tgg(pages["Combat"], "Auto Reset: Murderer", 4, "autoResetMurderer")
    tgg(pages["Combat"], "Auto Reset: Sheriff", 5, "autoResetSheriff")
    tgg(pages["Combat"], "Auto Reset: Innocent", 6, "autoResetInnocent")

    addSection(pages["Combat"], "Player Picker", 7)

    local pickerExpanded = false
    local pickerRow = Instance.new("Frame")
    pickerRow.Size = UDim2.new(1, 0, 0, 38 * selectedScale)
    pickerRow.LayoutOrder = 8
    pickerRow.BackgroundColor3 = CARD
    pickerRow.Parent = pages["Combat"]
    Instance.new("UICorner", pickerRow).CornerRadius = r(10)
    local prStroke = Instance.new("UIStroke", pickerRow)
    prStroke.Color = BORDER; prStroke.Transparency = 0.55

    local prLabel = Instance.new("TextLabel")
    prLabel.Size = UDim2.new(1, -60 * selectedScale, 1, 0)
    prLabel.Position = UDim2.new(0, 14 * selectedScale, 0, 0)
    prLabel.BackgroundTransparency = 1
    prLabel.Text = "Tap to open player list"
    prLabel.TextColor3 = TEXT
    prLabel.Font = Enum.Font.GothamMedium; prLabel.TextSize = 12.5 * fontSizeMult
    prLabel.TextXAlignment = Enum.TextXAlignment.Left; prLabel.Parent = pickerRow

    local prArrow = Instance.new("TextLabel")
    prArrow.Size = UDim2.new(0, 30 * selectedScale, 1, 0)
    prArrow.Position = UDim2.new(1, -40 * selectedScale, 0, 0)
    prArrow.BackgroundTransparency = 1
    prArrow.Text = "v"; prArrow.TextColor3 = TEXT_DIM
    prArrow.Font = Enum.Font.GothamBold; prArrow.TextSize = 14 * fontSizeMult
    prArrow.Parent = pickerRow

    local prHit = Instance.new("TextButton")
    prHit.Size = UDim2.new(1, 0, 1, 0)
    prHit.BackgroundTransparency = 1; prHit.Text = ""
    prHit.AutoButtonColor = false; prHit.Parent = pickerRow

    prHit.MouseEnter:Connect(function()
        TweenService:Create(pickerRow, TweenInfo.new(0.25), {BackgroundColor3 = CARD_HOVER}):Play()
        TweenService:Create(prStroke, TweenInfo.new(0.24), {Transparency = 0.15}):Play()
    end)
    prHit.MouseLeave:Connect(function()
        TweenService:Create(pickerRow, TweenInfo.new(0.25), {BackgroundColor3 = CARD}):Play()
        TweenService:Create(prStroke, TweenInfo.new(0.24), {Transparency = 0.55}):Play()
    end)

    local pickerContainer = Instance.new("Frame")
    pickerContainer.Size = UDim2.new(1, 0, 0, 0)
    pickerContainer.LayoutOrder = 9
    pickerContainer.BackgroundColor3 = CARD
    pickerContainer.ClipsDescendants = true; pickerContainer.Parent = pages["Combat"]
    Instance.new("UICorner", pickerContainer).CornerRadius = r(10)
    local pcStroke = Instance.new("UIStroke", pickerContainer)
    pcStroke.Color = BORDER; pcStroke.Transparency = 0.55

    local pickerScroll = Instance.new("ScrollingFrame")
    pickerScroll.Size = UDim2.new(1, -8 * selectedScale, 1, -8 * selectedScale)
    pickerScroll.Position = UDim2.new(0, 4 * selectedScale, 0, 4 * selectedScale)
    pickerScroll.BackgroundTransparency = 1
    pickerScroll.ScrollBarThickness = 3 * selectedScale
    pickerScroll.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 60)
    pickerScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    pickerScroll.Parent = pickerContainer

    local pickerLayout = Instance.new("UIListLayout")
    pickerLayout.SortOrder = Enum.SortOrder.LayoutOrder
    pickerLayout.Padding = UDim.new(0, 4 * selectedScale)
    pickerLayout.Parent = pickerScroll
    pickerLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        pickerScroll.CanvasSize = UDim2.new(0, 0, 0, pickerLayout.AbsoluteContentSize.Y + 4)
    end)

    local PICKER_EXPANDED = 200 * selectedScale

    prHit.MouseButton1Click:Connect(function()
        pickerExpanded = not pickerExpanded
        local targetY = pickerExpanded and PICKER_EXPANDED or 0
        TweenService:Create(pickerContainer, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(1, 0, 0, targetY)
        }):Play()
        TweenService:Create(prArrow, TweenInfo.new(0.42, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Rotation = pickerExpanded and 180 or 0
        }):Play()
        prLabel.Text = pickerExpanded and "Tap to close player list" or "Tap to open player list"
    end)

    addSection(pages["Combat"], "Custom Targets", 10)
    local flingListDisplay = Instance.new("Frame")
    flingListDisplay.Size = UDim2.new(1, 0, 0, 30 * selectedScale)
    flingListDisplay.LayoutOrder = 11
    flingListDisplay.BackgroundColor3 = CARD
    flingListDisplay.ClipsDescendants = true; flingListDisplay.Parent = pages["Combat"]
    Instance.new("UICorner", flingListDisplay).CornerRadius = r(10)
    local fldStroke = Instance.new("UIStroke", flingListDisplay)
    fldStroke.Color = BORDER; fldStroke.Transparency = 0.55

    local flingListInner = Instance.new("Frame")
    flingListInner.Size = UDim2.new(1, -12 * selectedScale, 0, 0)
    flingListInner.Position = UDim2.new(0, 6 * selectedScale, 0, 6 * selectedScale)
    flingListInner.BackgroundTransparency = 1
    flingListInner.AutomaticSize = Enum.AutomaticSize.Y
    flingListInner.Parent = flingListDisplay

    local flingListLayout = Instance.new("UIListLayout")
    flingListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    flingListLayout.Padding = UDim.new(0, 4 * selectedScale)
    flingListLayout.Parent = flingListInner
    flingListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        local contentY = flingListLayout.AbsoluteContentSize.Y + 12 * selectedScale
        if contentY < 30 * selectedScale then contentY = 30 * selectedScale end
        flingListDisplay.Size = UDim2.new(1, 0, 0, contentY)
    end)

    addSection(pages["Combat"], "Whitelist (Skipped by Fling All)", 12)
    local whitelistDisplay = Instance.new("Frame")
    whitelistDisplay.Size = UDim2.new(1, 0, 0, 30 * selectedScale)
    whitelistDisplay.LayoutOrder = 13
    whitelistDisplay.BackgroundColor3 = CARD
    whitelistDisplay.ClipsDescendants = true; whitelistDisplay.Parent = pages["Combat"]
    Instance.new("UICorner", whitelistDisplay).CornerRadius = r(10)
    local wlStroke = Instance.new("UIStroke", whitelistDisplay)
    wlStroke.Color = BORDER; wlStroke.Transparency = 0.55

    local whitelistInner = Instance.new("Frame")
    whitelistInner.Size = UDim2.new(1, -12 * selectedScale, 0, 0)
    whitelistInner.Position = UDim2.new(0, 6 * selectedScale, 0, 6 * selectedScale)
    whitelistInner.BackgroundTransparency = 1
    whitelistInner.AutomaticSize = Enum.AutomaticSize.Y
    whitelistInner.Parent = whitelistDisplay

    local whitelistLayout = Instance.new("UIListLayout")
    whitelistLayout.SortOrder = Enum.SortOrder.LayoutOrder
    whitelistLayout.Padding = UDim.new(0, 4 * selectedScale)
    whitelistLayout.Parent = whitelistInner
    whitelistLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        local contentY = whitelistLayout.AbsoluteContentSize.Y + 12 * selectedScale
        if contentY < 30 * selectedScale then contentY = 30 * selectedScale end
        whitelistDisplay.Size = UDim2.new(1, 0, 0, contentY)
    end)

    local function makeEmptyLabel(parent, text)
        local l = Instance.new("TextLabel")
        l.Size = UDim2.new(1, 0, 0, 26 * selectedScale)
        l.BackgroundTransparency = 1
        l.Text = text; l.TextColor3 = TEXT_DIM
        l.Font = Enum.Font.Gotham; l.TextSize = 11 * fontSizeMult
        l.TextXAlignment = Enum.TextXAlignment.Left; l.Parent = parent
        Instance.new("UIPadding", l).PaddingLeft = UDim.new(0, 8 * selectedScale)
        return l
    end

    local function rebuildPickerList()
        for _, c in ipairs(pickerScroll:GetChildren()) do
            if c:IsA("GuiObject") and c ~= pickerLayout then c:Destroy() end
        end
        local order = 0
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= LP then
                order = order + 1
                local row = Instance.new("Frame")
                row.Size = UDim2.new(1, -4 * selectedScale, 0, 30 * selectedScale)
                row.LayoutOrder = order
                row.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
                row.Parent = pickerScroll
                Instance.new("UICorner", row).CornerRadius = r(7)
                row.BackgroundTransparency = 1
                row.Position = UDim2.new(0.15, 0, 0, 0)
                task.delay(order * 0.035, function()
                    if row.Parent then
                        TweenService:Create(row, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                            BackgroundTransparency = 0, Position = UDim2.new(0, 0, 0, 0)
                        }):Play()
                    end
                end)

                local nameLbl = Instance.new("TextLabel")
                nameLbl.Size = UDim2.new(1, -130 * selectedScale, 1, 0)
                nameLbl.Position = UDim2.new(0, 10 * selectedScale, 0, 0)
                nameLbl.BackgroundTransparency = 1
                nameLbl.Text = plr.Name; nameLbl.TextColor3 = TEXT
                nameLbl.Font = Enum.Font.GothamMedium; nameLbl.TextSize = 11.5 * fontSizeMult
                nameLbl.TextXAlignment = Enum.TextXAlignment.Left
                nameLbl.TextTruncate = Enum.TextTruncate.AtEnd; nameLbl.Parent = row

                local isFling = isInList(settingsConfig.flingList, plr.Name)
                local isWhite = isInList(settingsConfig.whitelist, plr.Name)

                local flingBtn = Instance.new("TextButton")
                flingBtn.Size = UDim2.new(0, 60 * selectedScale, 0, 24 * selectedScale)
                flingBtn.Position = UDim2.new(1, -126 * selectedScale, 0.5, -12 * selectedScale)
                flingBtn.BackgroundColor3 = isFling and Color3.fromRGB(140, 50, 200) or Color3.fromRGB(30, 30, 30)
                flingBtn.Text = "Target"; flingBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                flingBtn.Font = Enum.Font.GothamBold; flingBtn.TextSize = 9.5 * fontSizeMult
                flingBtn.ClipsDescendants = true; flingBtn.Parent = row
                Instance.new("UICorner", flingBtn).CornerRadius = r(6)
                attachClickBounce(flingBtn); attachRipple(flingBtn)

                local whiteBtn = Instance.new("TextButton")
                whiteBtn.Size = UDim2.new(0, 60 * selectedScale, 0, 24 * selectedScale)
                whiteBtn.Position = UDim2.new(1, -62 * selectedScale, 0.5, -12 * selectedScale)
                whiteBtn.BackgroundColor3 = isWhite and Color3.fromRGB(60, 140, 220) or Color3.fromRGB(30, 30, 30)
                whiteBtn.Text = "Safe"; whiteBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                whiteBtn.Font = Enum.Font.GothamBold; whiteBtn.TextSize = 9.5 * fontSizeMult
                whiteBtn.ClipsDescendants = true; whiteBtn.Parent = row
                Instance.new("UICorner", whiteBtn).CornerRadius = r(6)
                attachClickBounce(whiteBtn); attachRipple(whiteBtn)

                flingBtn.MouseButton1Click:Connect(function()
                    if isInList(settingsConfig.flingList, plr.Name) then
                        removeFromList(settingsConfig.flingList, plr.Name)
                    else
                        addToList(settingsConfig.flingList, plr.Name)
                        removeFromList(settingsConfig.whitelist, plr.Name)
                    end
                    saveSettings()
                    if refreshPlayerPickerFn then refreshPlayerPickerFn() end
                end)
                whiteBtn.MouseButton1Click:Connect(function()
                    if isInList(settingsConfig.whitelist, plr.Name) then
                        removeFromList(settingsConfig.whitelist, plr.Name)
                    else
                        addToList(settingsConfig.whitelist, plr.Name)
                        removeFromList(settingsConfig.flingList, plr.Name)
                    end
                    saveSettings()
                    if refreshPlayerPickerFn then refreshPlayerPickerFn() end
                end)
            end
        end
        if order == 0 then
            local empty = Instance.new("TextLabel")
            empty.Size = UDim2.new(1, 0, 0, 30 * selectedScale)
            empty.BackgroundTransparency = 1
            empty.Text = "No other players in server."
            empty.TextColor3 = TEXT_DIM
            empty.Font = Enum.Font.Gotham; empty.TextSize = 11 * fontSizeMult
            empty.LayoutOrder = 1; empty.Parent = pickerScroll
        end
    end

    local function rebuildFlingListDisplay()
        for _, c in ipairs(flingListInner:GetChildren()) do
            if c:IsA("GuiObject") and c ~= flingListLayout then c:Destroy() end
        end
        if #settingsConfig.flingList == 0 then makeEmptyLabel(flingListInner, "No players in target list."); return end
        for i, name in ipairs(settingsConfig.flingList) do
            local row = Instance.new("Frame")
            row.Size = UDim2.new(1, 0, 0, 26 * selectedScale)
            row.LayoutOrder = i
            row.BackgroundColor3 = Color3.fromRGB(30, 20, 40)
            row.Parent = flingListInner
            Instance.new("UICorner", row).CornerRadius = r(6)
            row.BackgroundTransparency = 1
            row.Position = UDim2.new(0.1, 0, 0, 0)
            task.delay(i * 0.03, function()
                if row.Parent then
                    TweenService:Create(row, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                        BackgroundTransparency = 0, Position = UDim2.new(0, 0, 0, 0)
                    }):Play()
                end
            end)
            local lbl = Instance.new("TextLabel")
            lbl.Size = UDim2.new(1, -36 * selectedScale, 1, 0)
            lbl.Position = UDim2.new(0, 10 * selectedScale, 0, 0)
            lbl.BackgroundTransparency = 1
            lbl.Text = name; lbl.TextColor3 = Color3.fromRGB(220, 180, 255)
            lbl.Font = Enum.Font.GothamMedium; lbl.TextSize = 11 * fontSizeMult
            lbl.TextXAlignment = Enum.TextXAlignment.Left; lbl.Parent = row
            local removeBtn = Instance.new("TextButton")
            removeBtn.Size = UDim2.new(0, 24 * selectedScale, 0, 20 * selectedScale)
            removeBtn.Position = UDim2.new(1, -28 * selectedScale, 0.5, -10 * selectedScale)
            removeBtn.BackgroundColor3 = Color3.fromRGB(160, 40, 40)
            removeBtn.Text = "X"; removeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            removeBtn.Font = Enum.Font.GothamBold; removeBtn.TextSize = 10 * fontSizeMult
            removeBtn.ClipsDescendants = true; removeBtn.Parent = row
            Instance.new("UICorner", removeBtn).CornerRadius = r(5)
            attachClickBounce(removeBtn); attachRipple(removeBtn)
            removeBtn.MouseButton1Click:Connect(function()
                removeFromList(settingsConfig.flingList, name); saveSettings()
                if refreshPlayerPickerFn then refreshPlayerPickerFn() end
            end)
        end
    end

    local function rebuildWhitelistDisplay()
        for _, c in ipairs(whitelistInner:GetChildren()) do
            if c:IsA("GuiObject") and c ~= whitelistLayout then c:Destroy() end
        end
        if #settingsConfig.whitelist == 0 then makeEmptyLabel(whitelistInner, "No players whitelisted."); return end
        for i, name in ipairs(settingsConfig.whitelist) do
            local row = Instance.new("Frame")
            row.Size = UDim2.new(1, 0, 0, 26 * selectedScale)
            row.LayoutOrder = i
            row.BackgroundColor3 = Color3.fromRGB(20, 26, 40)
            row.Parent = whitelistInner
            Instance.new("UICorner", row).CornerRadius = r(6)
            row.BackgroundTransparency = 1
            row.Position = UDim2.new(0.1, 0, 0, 0)
            task.delay(i * 0.03, function()
                if row.Parent then
                    TweenService:Create(row, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                        BackgroundTransparency = 0, Position = UDim2.new(0, 0, 0, 0)
                    }):Play()
                end
            end)
            local lbl = Instance.new("TextLabel")
            lbl.Size = UDim2.new(1, -36 * selectedScale, 1, 0)
            lbl.Position = UDim2.new(0, 10 * selectedScale, 0, 0)
            lbl.BackgroundTransparency = 1
            lbl.Text = name; lbl.TextColor3 = Color3.fromRGB(180, 210, 255)
            lbl.Font = Enum.Font.GothamMedium; lbl.TextSize = 11 * fontSizeMult
            lbl.TextXAlignment = Enum.TextXAlignment.Left; lbl.Parent = row
            local removeBtn = Instance.new("TextButton")
            removeBtn.Size = UDim2.new(0, 24 * selectedScale, 0, 20 * selectedScale)
            removeBtn.Position = UDim2.new(1, -28 * selectedScale, 0.5, -10 * selectedScale)
            removeBtn.BackgroundColor3 = Color3.fromRGB(160, 40, 40)
            removeBtn.Text = "X"; removeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            removeBtn.Font = Enum.Font.GothamBold; removeBtn.TextSize = 10 * fontSizeMult
            removeBtn.ClipsDescendants = true; removeBtn.Parent = row
            Instance.new("UICorner", removeBtn).CornerRadius = r(5)
            attachClickBounce(removeBtn); attachRipple(removeBtn)
            removeBtn.MouseButton1Click:Connect(function()
                removeFromList(settingsConfig.whitelist, name); saveSettings()
                if refreshPlayerPickerFn then refreshPlayerPickerFn() end
            end)
        end
    end

    refreshPlayerPickerFn = function()
        rebuildPickerList()
        rebuildFlingListDisplay()
        rebuildWhitelistDisplay()
    end
    refreshPlayerPickerFn()

    task.spawn(function()
        local lastCount = #Players:GetPlayers()
        while sg.Parent do
            task.wait(1)
            local c = #Players:GetPlayers()
            if c ~= lastCount then
                lastCount = c
                if refreshPlayerPickerFn then refreshPlayerPickerFn() end
            end
        end
    end)

    addActionButton(pages["Combat"], "Kill Custom Now", 14, killCustomNow)
    addActionButton(pages["Combat"], "Fling Custom Now", 15, flingCustomNow)
    addActionButton(pages["Combat"], "Grab Gun Now", 16, grabGunNow)
    addActionButton(pages["Combat"], "Kill All Now", 17, killAllNow)
    addActionButton(pages["Combat"], "Kill Sheriff Now", 18, killSheriffNow)
    addActionButton(pages["Combat"], "Fling All Now", 19, flingAllNow)
    addActionButton(pages["Combat"], "Fling Murderer Now", 20, flingMurdererNow)
    addActionButton(pages["Combat"], "Fling Sheriff Now", 21, flingSheriffNow)
    tgg(pages["Combat"], "Aimbot: Murderer", 22, "aimbotMurderer")
    tgg(pages["Combat"], "Aimbot: Sheriff", 23, "aimbotSheriff")
    tgg(pages["Combat"], "Loop Fling All", 24, "loopFlingAll")
    tgg(pages["Combat"], "Loop Fling Murderer", 25, "loopFlingMur")
    tgg(pages["Combat"], "Loop Fling Sheriff", 26, "loopFlingSheriff")
    tgg(pages["Combat"], "Loop Fling Custom", 27, "loopFlingCustom")

    -- TELEPORT
    addSection(pages["Teleport"], "Locations", 1)
    addActionButton(pages["Teleport"], "Teleport to Lobby", 2, teleportToLobby)
    addActionButton(pages["Teleport"], "Teleport to Map", 3, teleportToMap)
    addSection(pages["Teleport"], "Players", 4)
    addActionButton(pages["Teleport"], "Teleport to Murderer", 5, teleportToMurderer)
    addActionButton(pages["Teleport"], "Teleport to Sheriff", 6, teleportToSheriff)
    addActionButton(pages["Teleport"], "Teleport to Custom", 7, teleportToCustom)

    local tpNote = Instance.new("TextLabel")
    tpNote.Size = UDim2.new(1, 0, 0, 36 * selectedScale)
    tpNote.LayoutOrder = 8
    tpNote.BackgroundTransparency = 1
    tpNote.Text = "Custom goes to first alive player in Custom Targets list"
    tpNote.TextColor3 = TEXT_DIM
    tpNote.Font = Enum.Font.Gotham; tpNote.TextSize = 11 * fontSizeMult
    tpNote.TextXAlignment = Enum.TextXAlignment.Left
    tpNote.TextWrapped = true; tpNote.Parent = pages["Teleport"]

    -- ESP
    tgg(pages["ESP"], "Player ESP (Roles)", 1, "espPlayers")
    tgg(pages["ESP"], "Gun Drop ESP", 2, "espGunDrop")

    -- WEBHOOK
    tgg(pages["Webhook"], "Auto send based off of timer", 1, "sendOnFull")
    local intervals = {
        {text = "30s", seconds = 30}, {text = "1m", seconds = 60},
        {text = "5m", seconds = 300}, {text = "10m", seconds = 600}, {text = "20m", seconds = 1200}
    }
    local currentIntervalIndex = 1
    for i, v in ipairs(intervals) do if v.seconds == settingsConfig.webhookCooldown then currentIntervalIndex = i break end end
    local timerBtn = Instance.new("TextButton")
    timerBtn.Size = UDim2.new(1, 0, 0, 38 * selectedScale)
    timerBtn.LayoutOrder = 2
    timerBtn.BackgroundColor3 = CARD
    timerBtn.Text = "  Timer Interval: " .. intervals[currentIntervalIndex].text
    timerBtn.TextColor3 = TEXT
    timerBtn.Font = Enum.Font.GothamMedium; timerBtn.TextSize = 12.5 * fontSizeMult
    timerBtn.TextXAlignment = Enum.TextXAlignment.Left
    timerBtn.ClipsDescendants = true; timerBtn.Parent = pages["Webhook"]
    Instance.new("UICorner", timerBtn).CornerRadius = r(10)
    local timerStroke = Instance.new("UIStroke", timerBtn)
    timerStroke.Color = BORDER; timerStroke.Transparency = 0.55
    timerBtn.MouseEnter:Connect(function()
        TweenService:Create(timerBtn, TweenInfo.new(0.24), {BackgroundColor3 = CARD_HOVER}):Play()
        TweenService:Create(timerStroke, TweenInfo.new(0.24), {Transparency = 0.15}):Play()
    end)
    timerBtn.MouseLeave:Connect(function()
        TweenService:Create(timerBtn, TweenInfo.new(0.24), {BackgroundColor3 = CARD}):Play()
        TweenService:Create(timerStroke, TweenInfo.new(0.24), {Transparency = 0.55}):Play()
    end)
    attachClickBounce(timerBtn); attachShine(timerBtn); attachRipple(timerBtn)
    timerBtn.MouseButton1Click:Connect(function()
        currentIntervalIndex = currentIntervalIndex % #intervals + 1
        local selected = intervals[currentIntervalIndex]
        timerBtn.Text = "  Timer Interval: " .. selected.text
        settingsConfig.webhookCooldown = selected.seconds; saveSettings()
    end)

    local function makeInput(page, order, placeholder, value, onChange)
        local box = Instance.new("TextBox")
        box.Size = UDim2.new(1, 0, 0, 36 * selectedScale)
        box.LayoutOrder = order
        box.BackgroundColor3 = CARD
        box.PlaceholderText = placeholder
        box.PlaceholderColor3 = TEXT_DIM
        box.Text = value or ""; box.TextColor3 = TEXT
        box.Font = Enum.Font.Gotham; box.TextSize = 12 * fontSizeMult
        box.ClearTextOnFocus = false; box.Parent = page
        Instance.new("UICorner", box).CornerRadius = r(10)
        local s = Instance.new("UIStroke", box)
        s.Color = BORDER; s.Transparency = 0.55
        local p = Instance.new("UIPadding", box)
        p.PaddingLeft = UDim.new(0, 12 * selectedScale)
        box.Focused:Connect(function() TweenService:Create(s, TweenInfo.new(0.22), {Color = Color3.fromRGB(120, 200, 255), Transparency = 0}):Play() end)
        box.FocusLost:Connect(function()
            TweenService:Create(s, TweenInfo.new(0.22), {Color = BORDER, Transparency = 0.55}):Play()
            onChange(box.Text)
        end)
        return box
    end
    makeInput(pages["Webhook"], 3, "Webhook URL...", settingsConfig.webhookUrl, function(t) settingsConfig.webhookUrl = t; saveSettings() end)
    makeInput(pages["Webhook"], 4, "Discord User ID (for ping)...", settingsConfig.discordUserId, function(t) settingsConfig.discordUserId = t; saveSettings() end)

    -- MISC
    tgg(pages["Misc"], "Anti AFK", 1, "afk")
    tgg(pages["Misc"], "Auto Grab Gun", 2, "gun")
    tgg(pages["Misc"], "Auto Rejoin", 3, "autoRejoin")
    tgg(pages["Misc"], "Auto Serverhop (<= 2)", 4, "autoServerHop")
    tgg(pages["Misc"], "Auto Prestige (Level 100)", 5, "autoPrestige")
    tgg(pages["Misc"], "Anti-Void", 6, "antiVoid")
    tgg(pages["Misc"], "Notify Map Name", 7, "notifyMapName")
    tgg(pages["Misc"], "Round Timer", 8, "showRoundTimerCountdown")

    -- UI
    tgg(pages["UI"], "Show Quick Toggles Menu", 1, "quickTogglesVisible")

    local scaleRow = Instance.new("Frame")
    scaleRow.Size = UDim2.new(1, 0, 0, 40 * selectedScale)
    scaleRow.LayoutOrder = 2
    scaleRow.BackgroundColor3 = CARD
    scaleRow.Parent = pages["UI"]
    Instance.new("UICorner", scaleRow).CornerRadius = r(10)
    local scaleStroke = Instance.new("UIStroke", scaleRow)
    scaleStroke.Color = BORDER; scaleStroke.Transparency = 0.55

    local scaleLbl = Instance.new("TextLabel")
    scaleLbl.Size = UDim2.new(0.5, 0, 1, 0)
    scaleLbl.Position = UDim2.new(0, 14 * selectedScale, 0, 0)
    scaleLbl.BackgroundTransparency = 1
    scaleLbl.Text = "UI Scale"; scaleLbl.TextColor3 = TEXT
    scaleLbl.Font = Enum.Font.GothamMedium; scaleLbl.TextSize = 12.5 * fontSizeMult
    scaleLbl.TextXAlignment = Enum.TextXAlignment.Left; scaleLbl.Parent = scaleRow

    local scaleMinus = makeStepper(scaleRow, -100 * selectedScale, "-")
    local scaleVal = Instance.new("TextLabel")
    scaleVal.Size = UDim2.new(0, 34 * selectedScale, 0, 26 * selectedScale)
    scaleVal.Position = UDim2.new(1, -66 * selectedScale, 0.5, -13 * selectedScale)
    scaleVal.BackgroundTransparency = 1
    scaleVal.Text = string.format("%.1fx", selectedScale)
    scaleVal.TextColor3 = TEXT
    scaleVal.Font = Enum.Font.GothamBold; scaleVal.TextSize = 11.5 * fontSizeMult
    scaleVal.Parent = scaleRow
    local scalePlus = makeStepper(scaleRow, -30 * selectedScale, "+")

    scaleMinus.MouseButton1Click:Connect(function()
        if selectedScale > 0.7 then
            selectedScale = math.floor((selectedScale - 0.1) * 10 + 0.5) / 10
            settingsConfig.selectedScale = selectedScale; saveSettings(); buildUI(true)
        end
    end)
    scalePlus.MouseButton1Click:Connect(function()
        if selectedScale < 1.5 then
            selectedScale = math.floor((selectedScale + 0.1) * 10 + 0.5) / 10
            settingsConfig.selectedScale = selectedScale; saveSettings(); buildUI(true)
        end
    end)

    addSection(pages["UI"], "Manage Quick Toggles", 3)

    local quickSearchBox = Instance.new("TextBox")
    quickSearchBox.Size = UDim2.new(1, 0, 0, 34 * selectedScale)
    quickSearchBox.LayoutOrder = 4
    quickSearchBox.BackgroundColor3 = CARD
    quickSearchBox.PlaceholderText = "Search feature..."
    quickSearchBox.PlaceholderColor3 = TEXT_DIM
    quickSearchBox.Text = ""; quickSearchBox.TextColor3 = TEXT
    quickSearchBox.Font = Enum.Font.Gotham; quickSearchBox.TextSize = 12 * fontSizeMult
    quickSearchBox.ClearTextOnFocus = false; quickSearchBox.Parent = pages["UI"]
    Instance.new("UICorner", quickSearchBox).CornerRadius = r(10)
    local qsStroke = Instance.new("UIStroke", quickSearchBox)
    qsStroke.Color = BORDER; qsStroke.Transparency = 0.55
    local qsPad = Instance.new("UIPadding", quickSearchBox)
    qsPad.PaddingLeft = UDim.new(0, 12 * selectedScale)

    local quickManagerScroll = Instance.new("ScrollingFrame")
    quickManagerScroll.Size = UDim2.new(1, 0, 0, 140 * selectedScale)
    quickManagerScroll.LayoutOrder = 5
    quickManagerScroll.BackgroundTransparency = 1
    quickManagerScroll.ScrollBarThickness = 3 * selectedScale
    quickManagerScroll.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 60)
    quickManagerScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    quickManagerScroll.Parent = pages["UI"]

    local qmLayout = Instance.new("UIListLayout")
    qmLayout.SortOrder = Enum.SortOrder.LayoutOrder
    qmLayout.Padding = UDim.new(0, 5 * selectedScale)
    qmLayout.Parent = quickManagerScroll
    qmLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        quickManagerScroll.CanvasSize = UDim2.new(0, 0, 0, qmLayout.AbsoluteContentSize.Y + 8)
    end)

    local allAvailableFeatures = {
        "Teleport to Lobby", "Teleport to Map",
        "Teleport to Murderer", "Teleport to Sheriff", "Teleport to Custom",
        "Grab Gun Now", "Kill All Now", "Kill Sheriff Now", "Kill Custom Now",
        "Fling Custom Now", "Fling All Now", "Fling Murderer Now", "Fling Sheriff Now",
        "Aimbot Murderer", "Aimbot Sheriff",
        "Loop Fling All", "Loop Fling Murderer", "Loop Fling Sheriff", "Loop Fling Custom",
        "Auto Shoot Murderer", "Auto Kill All", "Auto Fling Murderer",
        "Auto Reset Murderer", "Auto Reset Sheriff", "Auto Reset Innocent",
        "Farm (Safe)", "XP Farm", "Disable 3D Rendering", "FPS Boost (Potato)",
        "Player ESP (Roles)", "Gun Drop ESP",
        "Anti AFK", "Anti-Void", "Notify Map Name", "Round Timer",
        "Auto Grab Gun", "Auto Rejoin", "Auto Serverhop", "Auto Prestige",
        "Show Quick Toggles Menu", "Fly", "Noclip", "Infinite Jump",
        "Speed Changer", "Jump Power", "Invisible (Ghost)",
        "Instant Interact", "Camera Max Zoom", "Info HUD",
        "Freeze (Anchor)",
        "Fullbright", "FOV Changer", "Click / Tap Teleport"
    }

    local function refreshQuickManager(filterText)
        for _, child in ipairs(quickManagerScroll:GetChildren()) do
            if child:IsA("GuiObject") and child ~= qmLayout then child:Destroy() end
        end
        filterText = string.lower(filterText or "")
        for _, featName in ipairs(allAvailableFeatures) do
            if filterText == "" or string.find(string.lower(featName), filterText) then
                local isAlreadyAdded = false
                for _, addedName in ipairs(settingsConfig.quickTogglesList) do
                    if addedName == featName then isAlreadyAdded = true break end
                end
                local row = Instance.new("Frame")
                row.Size = UDim2.new(1, 0, 0, 32 * selectedScale)
                row.BackgroundColor3 = CARD
                row.Parent = quickManagerScroll
                Instance.new("UICorner", row).CornerRadius = r(8)
                local rs = Instance.new("UIStroke", row)
                rs.Color = BORDER; rs.Transparency = 0.55

                local nameLbl = Instance.new("TextLabel")
                nameLbl.Size = UDim2.new(0.62, 0, 1, 0)
                nameLbl.Position = UDim2.new(0, 10 * selectedScale, 0, 0)
                nameLbl.BackgroundTransparency = 1
                nameLbl.Text = featName; nameLbl.TextColor3 = TEXT
                nameLbl.Font = Enum.Font.GothamMedium; nameLbl.TextSize = 11.5 * fontSizeMult
                nameLbl.TextXAlignment = Enum.TextXAlignment.Left; nameLbl.Parent = row

                local actionBtn = Instance.new("TextButton")
                actionBtn.Size = UDim2.new(0, 62 * selectedScale, 0, 24 * selectedScale)
                actionBtn.Position = UDim2.new(1, -70 * selectedScale, 0.5, -12 * selectedScale)
                actionBtn.BackgroundColor3 = isAlreadyAdded and Color3.fromRGB(160, 40, 40) or Color3.fromRGB(35, 140, 65)
                actionBtn.Text = isAlreadyAdded and "Remove" or "Add"
                actionBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                actionBtn.Font = Enum.Font.GothamBold; actionBtn.TextSize = 10.5 * fontSizeMult
                actionBtn.ClipsDescendants = true; actionBtn.Parent = row
                Instance.new("UICorner", actionBtn).CornerRadius = r(6)
                attachClickBounce(actionBtn); attachRipple(actionBtn)
                actionBtn.MouseButton1Click:Connect(function()
                    if isAlreadyAdded then
                        for idx, addedName in ipairs(settingsConfig.quickTogglesList) do
                            if addedName == featName then table.remove(settingsConfig.quickTogglesList, idx) break end
                        end
                    else
                        table.insert(settingsConfig.quickTogglesList, featName)
                    end
                    saveSettings()
                    task.defer(function()
                        if refreshQuickTogglesPanel then refreshQuickTogglesPanel() end
                        if refreshQuickManagerFn then refreshQuickManagerFn(quickSearchBox.Text) end
                    end)
                end)
            end
        end
    end
    quickSearchBox:GetPropertyChangedSignal("Text"):Connect(function() refreshQuickManager(quickSearchBox.Text) end)
    refreshQuickManager("")
    refreshQuickManagerFn = refreshQuickManager

    -- VOTING
    local function makeSpotLabel(order, text)
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(1, 0, 0, 24 * selectedScale)
        lbl.LayoutOrder = order
        lbl.BackgroundTransparency = 1
        lbl.Text = text; lbl.TextColor3 = TEXT
        lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 12.5 * fontSizeMult
        lbl.TextXAlignment = Enum.TextXAlignment.Left; lbl.Parent = pages["Voting"]
        return lbl
    end

    local Spot1Label = makeSpotLabel(1, "Loading Slot 1...")
    addActionButton(pages["Voting"], "Teleport Spot 1", 2, function() tpToSpot(1) end)
    addActionButton(pages["Voting"], "Reset x4 (Map 1)", 3, function() resetX4(1) end)
    local Spot2Label = makeSpotLabel(4, "Loading Slot 2...")
    addActionButton(pages["Voting"], "Teleport Spot 2", 5, function() tpToSpot(2) end)
    addActionButton(pages["Voting"], "Reset x4 (Map 2)", 6, function() resetX4(2) end)
    local Spot3Label = makeSpotLabel(7, "Loading Slot 3...")
    addActionButton(pages["Voting"], "Teleport Spot 3", 8, function() tpToSpot(3) end)
    addActionButton(pages["Voting"], "Reset x4 (Map 3)", 9, function() resetX4(3) end)

    task.spawn(function()
        while true do
            task.wait(1)
            if not sg.Parent then break end
            pcall(function()
                local names = getMapNames()
                if Spot1Label and Spot1Label.Parent then Spot1Label.Text = names[1] end
                if Spot2Label and Spot2Label.Parent then Spot2Label.Text = names[2] end
                if Spot3Label and Spot3Label.Parent then Spot3Label.Text = names[3] end
            end)
        end
    end)

    -- Open button
    local fb = Instance.new("TextButton")
    fb.Size = UDim2.new(0, 78 * selectedScale, 0, 34 * selectedScale)
    fb.Position = UDim2.new(0, 14 * selectedScale, 0.28, 0)
    fb.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
    fb.Text = "Open"; fb.TextColor3 = WHITE_HEX
    fb.Font = Enum.Font.GothamBold; fb.TextSize = 12.5 * fontSizeMult
    fb.TextStrokeTransparency = 1
    fb.ClipsDescendants = true; fb.Parent = sg
    fb.ZIndex = 99999; fb.Active = true; fb.Draggable = true
    Instance.new("UICorner", fb).CornerRadius = r(9)
    local fbStroke = Instance.new("UIStroke", fb)
    fbStroke.Color = BORDER; fbStroke.Transparency = 0.3
    attachClickBounce(fb); attachHoverLift(fb, 1.05)
    attachShine(fb, 0.6); attachRipple(fb)

    fb.MouseEnter:Connect(function()
        TweenService:Create(fbStroke, TweenInfo.new(0.24), {Transparency = 0}):Play()
        TweenService:Create(fb, TweenInfo.new(0.24, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {BackgroundColor3 = Color3.fromRGB(24, 24, 24)}):Play()
    end)
    fb.MouseLeave:Connect(function()
        TweenService:Create(fbStroke, TweenInfo.new(0.24), {Transparency = 0.3}):Play()
        TweenService:Create(fb, TweenInfo.new(0.24), {BackgroundColor3 = Color3.fromRGB(12, 12, 12)}):Play()
    end)

    local isOpen = false
    local function openUI()
        isOpen = true
        main.Visible = true
        main.Size = UDim2.new(0, 0, 0, 0)
        main.Position = UDim2.new(0.5, 0, 0.5, 0)
        main.BackgroundTransparency = 0.6
        TweenService:Create(main, TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, baseWidth, 0, baseHeight)}):Play()
        TweenService:Create(main, TweenInfo.new(0.5, Enum.EasingStyle.Quad), {BackgroundTransparency = 0}):Play()
        fb.Text = "Close"; fb.TextColor3 = WHITE_HEX
    end
    local function closeUI()
        isOpen = false
        TweenService:Create(main, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0)}):Play()
        task.delay(0.4, function() if not isOpen then main.Visible = false end end)
        fb.Text = "Open"; fb.TextColor3 = WHITE_HEX
    end
    if rebuilding then openUI() end
    fb.MouseButton1Click:Connect(function() if isOpen then closeUI() else openUI() end end)

    -- Quick Toggles panel
    local quickFrame = Instance.new("Frame")
    quickFrame.Size = UDim2.new(0, 158 * selectedScale, 0, 270 * selectedScale)
    quickFrame.Position = UDim2.new(0.86, -168, 0.36, 0)
    quickFrame.BackgroundColor3 = BG
    quickFrame.Active = true; quickFrame.Draggable = true
    quickFrame.Visible = state.quickTogglesVisible
    quickFrame.Parent = sg
    Instance.new("UICorner", quickFrame).CornerRadius = r(12)
    local qfStroke = Instance.new("UIStroke", quickFrame)
    qfStroke.Color = BORDER; qfStroke.Transparency = 0.35
    quickFrameReference = quickFrame

    local qTitle = Instance.new("TextLabel")
    qTitle.Size = UDim2.new(1, 0, 0, 32 * selectedScale)
    qTitle.BackgroundTransparency = 1
    qTitle.Text = "Quick Toggles"; qTitle.TextColor3 = TEXT
    qTitle.Font = Enum.Font.GothamBold; qTitle.TextSize = 13 * selectedScale
    qTitle.Parent = quickFrame

    local qLine = Instance.new("Frame")
    qLine.Size = UDim2.new(1, -20 * selectedScale, 0, 1)
    qLine.Position = UDim2.new(0, 10 * selectedScale, 0, 32 * selectedScale)
    qLine.BackgroundColor3 = BORDER; qLine.BackgroundTransparency = 0.4
    qLine.Parent = quickFrame

    local quickScroll = Instance.new("ScrollingFrame")
    quickScroll.Size = UDim2.new(1, -12 * selectedScale, 1, -42 * selectedScale)
    quickScroll.Position = UDim2.new(0, 6 * selectedScale, 0, 38 * selectedScale)
    quickScroll.BackgroundTransparency = 1
    quickScroll.ScrollBarThickness = 2 * selectedScale
    quickScroll.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 60)
    quickScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    quickScroll.Parent = quickFrame

    local qLayout = Instance.new("UIListLayout")
    qLayout.SortOrder = Enum.SortOrder.LayoutOrder
    qLayout.Padding = UDim.new(0, 5 * selectedScale)
    qLayout.Parent = quickScroll
    qLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        quickScroll.CanvasSize = UDim2.new(0, 0, 0, qLayout.AbsoluteContentSize.Y + 8)
    end)

    local function createQuickItem(name, order)
        local stateKeyMap = {
            ["Aimbot Murderer"] = "aimbotMurderer", ["Aimbot Sheriff"] = "aimbotSheriff",
            ["Loop Fling All"] = "loopFlingAll", ["Loop Fling Murderer"] = "loopFlingMur",
            ["Loop Fling Sheriff"] = "loopFlingSheriff", ["Loop Fling Custom"] = "loopFlingCustom",
            ["Auto Shoot Murderer"] = "autoShootMur", ["Auto Kill All"] = "autoKillAll",
            ["Auto Fling Murderer"] = "autoFlingMur", ["Auto Reset Murderer"] = "autoResetMurderer",
            ["Auto Reset Sheriff"] = "autoResetSheriff", ["Auto Reset Innocent"] = "autoResetInnocent",
            ["Farm (Safe)"] = "farm", ["XP Farm"] = "xpFarm",
            ["Disable 3D Rendering"] = "disable3d", ["FPS Boost (Potato)"] = "fpsBoost",
            ["Player ESP (Roles)"] = "espPlayers", ["Gun Drop ESP"] = "espGunDrop",
            ["Anti AFK"] = "afk", ["Anti-Void"] = "antiVoid", ["Auto Grab Gun"] = "gun",
            ["Auto Rejoin"] = "autoRejoin", ["Auto Serverhop"] = "autoServerHop",
            ["Auto Prestige"] = "autoPrestige", ["Show Quick Toggles Menu"] = "quickTogglesVisible",
            ["Notify Map Name"] = "notifyMapName", ["Round Timer"] = "showRoundTimerCountdown",
            ["Fly"] = "flyEnabled", ["Noclip"] = "noclip", ["Infinite Jump"] = "infJumpEnabled",
            ["Speed Changer"] = "speedEnabled", ["Jump Power"] = "jumpEnabled", ["Invisible (Ghost)"] = "invisibleEnabled",
            ["Instant Interact"] = "instantInteract",
            ["Camera Max Zoom"] = "cameraZoomEnabled", ["Info HUD"] = "infoHudEnabled",
            ["Freeze (Anchor)"] = "freeze", ["Fullbright"] = "fullbright",
            ["FOV Changer"] = "fovEnabled", ["Click / Tap Teleport"] = "clickTP",
        }
        local stateKey = stateKeyMap[name]
        local isToggle = stateKey ~= nil
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 28 * selectedScale)
        btn.LayoutOrder = order
        btn.BackgroundColor3 = isToggle and (state[stateKey] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(18, 18, 18)) or Color3.fromRGB(18, 18, 18)
        btn.Text = name
        btn.TextColor3 = isToggle and (state[stateKey] and Color3.fromRGB(0, 0, 0) or TEXT) or TEXT
        btn.Font = Enum.Font.GothamMedium; btn.TextSize = 10.5 * selectedScale
        btn.ClipsDescendants = true; btn.Parent = quickScroll
        Instance.new("UICorner", btn).CornerRadius = r(7)
        local qStroke = Instance.new("UIStroke", btn)
        qStroke.Color = BORDER; qStroke.Transparency = 0.55
        btn.MouseEnter:Connect(function()
            if not (isToggle and state[stateKey]) then
                TweenService:Create(btn, TweenInfo.new(0.18, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {BackgroundColor3 = Color3.fromRGB(30, 30, 30)}):Play()
            end
            TweenService:Create(qStroke, TweenInfo.new(0.2), {Transparency = 0.15}):Play()
        end)
        btn.MouseLeave:Connect(function()
            if not (isToggle and state[stateKey]) then
                TweenService:Create(btn, TweenInfo.new(0.18), {BackgroundColor3 = Color3.fromRGB(18, 18, 18)}):Play()
            end
            TweenService:Create(qStroke, TweenInfo.new(0.2), {Transparency = 0.55}):Play()
        end)
        attachClickBounce(btn); attachRipple(btn)
        if isToggle then quickToggleButtons[stateKey] = btn end
        btn.MouseButton1Click:Connect(function()
            if isToggle then
                setToggleState(stateKey, not state[stateKey])
            else
                if name == "Grab Gun Now" then grabGunNow()
                elseif name == "Kill All Now" then killAllNow()
                elseif name == "Kill Sheriff Now" then killSheriffNow()
                elseif name == "Kill Custom Now" then killCustomNow()
                elseif name == "Fling Custom Now" then flingCustomNow()
                elseif name == "Fling All Now" then flingAllNow()
                elseif name == "Fling Murderer Now" then flingMurdererNow()
                elseif name == "Fling Sheriff Now" then flingSheriffNow()
                elseif name == "Teleport to Lobby" then teleportToLobby()
                elseif name == "Teleport to Map" then teleportToMap()
                elseif name == "Teleport to Murderer" then teleportToMurderer()
                elseif name == "Teleport to Sheriff" then teleportToSheriff()
                elseif name == "Teleport to Custom" then teleportToCustom() end
            end
        end)
    end

    refreshQuickTogglesPanel = function()
        for _, child in ipairs(quickScroll:GetChildren()) do
            if child:IsA("GuiObject") and child ~= qLayout then child:Destroy() end
        end
        for key, _ in pairs(quickToggleButtons) do quickToggleButtons[key] = nil end
        for idx, featName in ipairs(settingsConfig.quickTogglesList) do createQuickItem(featName, idx) end
    end

    if settingsConfig.quickTogglesList then
        for idx, featName in ipairs(settingsConfig.quickTogglesList) do createQuickItem(featName, idx) end
    end
end

buildUI()

if state.disable3d then applyLowDeviceOptimizations(true) end
if state.fpsBoost then applyFpsBoost(true) end

-- ==================== MOBILE FLY BUTTONS ====================
local mobileFlyUpBtn, mobileFlyDownBtn = nil, nil
local flyUp, flyDown = false, false

task.spawn(function()
    task.wait(1)
    local sg = getGuiParent():FindFirstChild("Appleware")
    if sg then
        local btnUp = Instance.new("TextButton")
        btnUp.Size = UDim2.new(0, 50, 0, 50)
        btnUp.Position = UDim2.new(1, -120, 0.5, 50)
        btnUp.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        btnUp.Text = "Fly +"; btnUp.TextColor3 = Color3.fromRGB(255, 255, 255)
        btnUp.Font = Enum.Font.Gotham; btnUp.TextSize = 10
        btnUp.Visible = false; btnUp.Parent = sg
        Instance.new("UICorner", btnUp).CornerRadius = UDim.new(1, 0)
        attachRipple(btnUp)

        local btnDown = Instance.new("TextButton")
        btnDown.Size = UDim2.new(0, 50, 0, 50)
        btnDown.Position = UDim2.new(1, -120, 0.5, 110)
        btnDown.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        btnDown.Text = "Fly -"; btnDown.TextColor3 = Color3.fromRGB(255, 255, 255)
        btnDown.Font = Enum.Font.Gotham; btnDown.TextSize = 10
        btnDown.Visible = false; btnDown.Parent = sg
        Instance.new("UICorner", btnDown).CornerRadius = UDim.new(1, 0)
        attachRipple(btnDown)

        btnUp.MouseButton1Down:Connect(function() flyUp = true end)
        btnUp.MouseButton1Up:Connect(function() flyUp = false end)
        btnUp.MouseLeave:Connect(function() flyUp = false end)
        btnDown.MouseButton1Down:Connect(function() flyDown = true end)
        btnDown.MouseButton1Up:Connect(function() flyDown = false end)
        btnDown.MouseLeave:Connect(function() flyDown = false end)

        mobileFlyUpBtn = btnUp
        mobileFlyDownBtn = btnDown
    end
end)

-- ==================== FLY LOOP ====================
local flyBV, flyBG = nil, nil
local wasFlying = false

local function cleanupFly()
    if mobileFlyUpBtn then mobileFlyUpBtn.Visible = false end
    if mobileFlyDownBtn then mobileFlyDownBtn.Visible = false end
    flyUp = false
    flyDown = false
    if flyBV then pcall(function() flyBV:Destroy() end) flyBV = nil end
    if flyBG then pcall(function() flyBG:Destroy() end) flyBG = nil end
    if wasFlying and LP.Character then
        local hum = LP.Character:FindFirstChildOfClass("Humanoid")
        local root = LP.Character:FindFirstChild("HumanoidRootPart")
        if hum and hum.Health > 0 then
            hum.PlatformStand = false
            hum.Sit = false
            hum.AutoRotate = true
            hum:ChangeState(Enum.HumanoidStateType.GettingUp)
            task.wait(0.1)
            if hum and hum.Health > 0 then
                hum:ChangeState(Enum.HumanoidStateType.Running)
            end
        end
        if root then
            root.AssemblyLinearVelocity = Vector3.zero
            root.AssemblyAngularVelocity = Vector3.zero
        end
    end
    wasFlying = false
end

LP.CharacterAdded:Connect(function()
    task.wait(0.5)
    if not state.flyEnabled then
        wasFlying = false
        flyBV = nil
        flyBG = nil
        flyUp = false
        flyDown = false
    end
end)

task.spawn(function()
    while true do
        task.wait(0.05)
        if state.flyEnabled and LP.Character then
            local root = LP.Character:FindFirstChild("HumanoidRootPart")
            local hum = LP.Character:FindFirstChildOfClass("Humanoid")
            if root and hum and hum.Health > 0 then
                wasFlying = true
                hum.PlatformStand = true

                if not flyBV or flyBV.Parent ~= root then
                    if flyBV then pcall(function() flyBV:Destroy() end) end
                    if flyBG then pcall(function() flyBG:Destroy() end) end
                    flyBV = Instance.new("BodyVelocity")
                    flyBV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                    flyBV.Velocity = Vector3.zero
                    flyBV.Parent = root
                    flyBG = Instance.new("BodyGyro")
                    flyBG.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
                    flyBG.P = 9e4
                    flyBG.Parent = root
                end

                if mobileFlyUpBtn then mobileFlyUpBtn.Visible = true end
                if mobileFlyDownBtn then mobileFlyDownBtn.Visible = true end

                local moveDir = Vector3.zero
                local camCF = Workspace.CurrentCamera.CFrame

                if UIS.KeyboardEnabled then
                    if UIS:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camCF.LookVector end
                    if UIS:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camCF.LookVector end
                    if UIS:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camCF.RightVector end
                    if UIS:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camCF.RightVector end
                    if UIS:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0,1,0) end
                    if UIS:IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0,1,0) end
                end

                if UIS.TouchEnabled then
                    local playerModule = LP.PlayerScripts:FindFirstChild("PlayerModule")
                    if playerModule then
                        pcall(function()
                            local controls = require(playerModule):GetControls()
                            local moveVector = controls:GetMoveVector()
                            if moveVector.Magnitude > 0 then
                                moveDir = moveDir + (camCF.LookVector * -moveVector.Z) + (camCF.RightVector * moveVector.X)
                            end
                        end)
                    end
                    if flyUp then moveDir = moveDir + Vector3.new(0,1,0) end
                    if flyDown then moveDir = moveDir - Vector3.new(0,1,0) end
                end

                flyBV.Velocity = moveDir * (state.flySpeed or 50)
                flyBG.CFrame = camCF
            else
                cleanupFly()
            end
        else
            cleanupFly()
        end
    end
end)

-- ==================== NOCLIP LOOP ====================
task.spawn(function()
    while true do
        task.wait(0.1)
        if state.noclip and LP.Character then
            for _, part in ipairs(LP.Character:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end
end)

-- ==================== INFINITE JUMP ====================
UIS.JumpRequest:Connect(function()
    if state.infJumpEnabled and LP.Character then
        local hum = LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

-- ==================== SPEED & JUMP POWER ====================
task.spawn(function()
    while true do
        task.wait(0.1)
        if LP.Character then
            local hum = LP.Character:FindFirstChildOfClass("Humanoid")
            if hum then
                if state.speedEnabled then hum.WalkSpeed = state.speedValue or 16
                else hum.WalkSpeed = 16 end
                if state.jumpEnabled then
                    hum.UseJumpPower = true
                    hum.JumpPower = state.jumpValue or 50
                else
                    hum.UseJumpPower = false
                end
            end
        end
    end
end)

-- ==================== INSTANT INTERACT ====================
task.spawn(function()
    while true do
        task.wait(0.15)
        if state.instantInteract and LP.Character then
            local root = LP.Character:FindFirstChild("HumanoidRootPart")
            if root then
                pcall(function()
                    for _, obj in ipairs(Workspace:GetDescendants()) do
                        if obj:IsA("ProximityPrompt") and obj.Enabled then
                            local parent = obj.Parent
                            if parent and (parent:IsA("BasePart") or parent:IsA("Attachment")) then
                                local pos = parent:IsA("BasePart") and parent.Position or parent.WorldPosition
                                if (pos - root.Position).Magnitude <= obj.MaxActivationDistance then
                                    pcall(function() fireproximityprompt(obj) end)
                                end
                            end
                        end
                    end
                end)
            end
        end
    end
end)

-- ==================== CAMERA MAX ZOOM ====================
task.spawn(function()
    while true do
        task.wait(0.3)
        pcall(function()
            if state.cameraZoomEnabled then
                LP.CameraMaxZoomDistance = state.cameraMaxZoom or 500
                LP.CameraMinZoomDistance = math.min(LP.CameraMinZoomDistance, 0.5)
            else
                LP.CameraMaxZoomDistance = 400
            end
        end)
    end
end)

-- ==================== SECRET FEATURES ====================
task.spawn(function()
    while true do
        task.wait(0.15)
        if LP.Character and not state.flyEnabled and not invisActive then
            local hum = LP.Character:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 and hum.PlatformStand and not next(activeResets) then
                hum.PlatformStand = false
                hum:ChangeState(Enum.HumanoidStateType.GettingUp)
            end
        end
    end
end)

local freezeApplied = false
task.spawn(function()
    while true do
        task.wait(0.1)
        if LP.Character then
            local root = LP.Character:FindFirstChild("HumanoidRootPart")
            if root then
                if state.freeze then
                    if not freezeApplied then
                        root.Anchored = true
                        freezeApplied = true
                    end
                else
                    if freezeApplied then
                        if not (state.xpFarm or (state.farm and (isLobby() or isExecutingAction))) then
                            root.Anchored = false
                        end
                        freezeApplied = false
                    end
                end
            end
        else
            freezeApplied = false
        end
    end
end)

-- FOV changer
local savedOriginalFOV = nil
task.spawn(function()
    task.wait(1)
    local cam = Workspace.CurrentCamera
    if cam then savedOriginalFOV = cam.FieldOfView end
end)

RunService.RenderStepped:Connect(function()
    local cam = Workspace.CurrentCamera
    if not cam then return end
    if state.fovEnabled then
        local target = state.fovValue or 70
        if math.abs(cam.FieldOfView - target) > 0.5 then
            cam.FieldOfView = target
        end
    else
        if savedOriginalFOV and math.abs(cam.FieldOfView - savedOriginalFOV) > 0.5 then
            cam.FieldOfView = savedOriginalFOV
        end
    end
end)

local function getTeleportTargetFromScreen(x, y)
    local cam = Workspace.CurrentCamera
    if not cam then return nil end
    local ray = cam:ViewportPointToRay(x, y)
    local params = RaycastParams.new()
    if LP.Character then
        params.FilterDescendantsInstances = {LP.Character}
    end
    params.FilterType = Enum.RaycastFilterType.Exclude
    local result = Workspace:Raycast(ray.Origin, ray.Direction * 2000, params)
    if result then return result.Position end
    return ray.Origin + ray.Direction * 60
end

local function doClickTeleport(x, y)
    if not state.clickTP then return end
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    local pos = getTeleportTargetFromScreen(x, y)
    if pos then
        root.CFrame = CFrame.new(pos + Vector3.new(0, 3, 0))
        root.AssemblyLinearVelocity = Vector3.zero
        root.AssemblyAngularVelocity = Vector3.zero
    end
end

UIS.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if not state.clickTP then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        local m = UIS:GetMouseLocation()
        doClickTeleport(m.X, m.Y)
    end
end)

UIS.TouchTap:Connect(function(positions, gpe)
    if gpe then return end
    if not state.clickTP then return end
    if positions and positions[1] then
        doClickTeleport(positions[1].X, positions[1].Y)
    end
end)

local savedPositions = {nil, nil, nil}

local function saveCurrentPosition(slot)
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if root then
        savedPositions[slot] = root.CFrame
        setStatus("Position saved to slot " .. slot)
    end
end

local function loadPosition(slot)
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if root and savedPositions[slot] then
        root.CFrame = savedPositions[slot]
        root.AssemblyLinearVelocity = Vector3.zero
        root.AssemblyAngularVelocity = Vector3.zero
        setStatus("Teleported to saved slot " .. slot)
    else
        setStatus("No saved position in slot " .. slot)
    end
end

-- ==================== INVISIBLE (GHOST MODE) ====================
local invisActive = false
local invisGhost = nil
local invisLoop = nil
local invisStaticPos = nil
local invisLastSafeCFrame = nil
local invisRealRoot = nil
local invisGhostRoot = nil

local _hookOK = false
pcall(function() _hookOK = (hookmetamethod ~= nil and checkcaller ~= nil and debug and debug.info) end)

if _hookOK then
    local _oldIndex
    _oldIndex = hookmetamethod(game, "__index", function(self, key)
        if not invisActive or checkcaller() or self ~= invisRealRoot then
            return _oldIndex(self, key)
        end
        for i = 2, 5 do
            local src = debug.info(i, "s")
            if src then
                local lower = string.lower(src)
                if string.find(lower, "cheat") or string.find(lower, "ac") or string.find(lower, "anti") or string.find(lower, "adonet") or string.find(lower, "detection") or string.find(lower, "security") then
                    return _oldIndex(self, key)
                end
            end
        end
        if invisGhostRoot and (key == "CFrame" or key == "Position") then
            if key == "CFrame" then return invisGhostRoot.CFrame
            elseif key == "Position" then return invisGhostRoot.Position end
        end
        return _oldIndex(self, key)
    end)

    local _oldNewIndex
    _oldNewIndex = hookmetamethod(game, "__newindex", function(self, key, value)
        if not invisActive or checkcaller() or self ~= invisRealRoot then
            return _oldNewIndex(self, key, value)
        end
        for i = 2, 5 do
            local src = debug.info(i, "s")
            if src then
                local lower = string.lower(src)
                if string.find(lower, "cheat") or string.find(lower, "ac") or string.find(lower, "anti") or string.find(lower, "adonet") or string.find(lower, "detection") or string.find(lower, "security") then
                    return _oldNewIndex(self, key, value)
                end
            end
        end
        if invisGhostRoot and (key == "CFrame" or key == "Position") then
            if key == "CFrame" then invisGhostRoot.CFrame = value
            elseif key == "Position" then invisGhostRoot.Position = value end
            return
        end
        return _oldNewIndex(self, key, value)
    end)
end

local function invisStop()
    if not invisActive and not invisGhost then return end
    invisActive = false
    local char = LP.Character
    local r = char and char:FindFirstChild("HumanoidRootPart")
    local h = char and char:FindFirstChildOfClass("Humanoid")
    local targetCFrame = (invisGhostRoot and invisGhostRoot.CFrame) or invisLastSafeCFrame
    if r and targetCFrame then
        r.CFrame = targetCFrame
        r.AssemblyLinearVelocity = Vector3.zero
        r.AssemblyAngularVelocity = Vector3.zero
    end
    if char then
        for _, v in char:GetChildren() do
            if v:IsA("BasePart") and v.Name ~= "HumanoidRootPart" then v.CanCollide = true
            elseif v:IsA("Accessory") then
                local handle = v:FindFirstChild("Handle")
                if handle then handle.CanCollide = false end
            end
        end
    end
    if h then
        local cam = Workspace.CurrentCamera
        if cam then cam.CameraSubject = h end
        h.PlatformStand = false
        h:SetStateEnabled(Enum.HumanoidStateType.Climbing, true)
        h:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
        h:SetStateEnabled(Enum.HumanoidStateType.Running, true)
        h.AutoRotate = true
        h.Jump = false
        h:ChangeState(Enum.HumanoidStateType.GettingUp)
    end
    if invisGhost then invisGhost:Destroy() invisGhost = nil end
    if invisLoop then invisLoop:Disconnect() invisLoop = nil end
    invisStaticPos = nil
    invisLastSafeCFrame = nil
    invisRealRoot = nil
    invisGhostRoot = nil
end

local function invisStart()
    if invisActive then return end
    local char = LP.Character
    if not char then return end
    local r = char:FindFirstChild("HumanoidRootPart")
    local h = char:FindFirstChildOfClass("Humanoid")
    if not r or not h or h.Health <= 0 then return end

    invisRealRoot = r
    invisStaticPos = CFrame.new(0, Workspace.FallenPartsDestroyHeight + 100, 0)
    invisLastSafeCFrame = r.CFrame

    char.Archivable = true
    invisGhost = char:Clone()
    invisGhost.Parent = Workspace
    char.Archivable = false

    for _, v in invisGhost:GetDescendants() do
        if v:IsA("BasePart") then v.Transparency = 0.5
        elseif v:IsA("Accessory") then
            local handle = v:FindFirstChild("Handle")
            if handle and handle:IsA("BasePart") then handle.Transparency = 0.5 end
        elseif v:IsA("LocalScript") or v:IsA("Script") then v:Destroy() end
    end

    local gh = invisGhost:FindFirstChild("Humanoid")
    invisGhostRoot = invisGhost:FindFirstChild("HumanoidRootPart")
    local cam = Workspace.CurrentCamera
    if gh and cam then cam.CameraSubject = gh end
    if gh then gh:SetStateEnabled(Enum.HumanoidStateType.Climbing, true) end

    h.PlatformStand = true
    r.CFrame = invisStaticPos
    for _, v in char:GetDescendants() do
        if v:IsA("BasePart") then v.CanCollide = false end
    end

    invisActive = true
    setStatus("Invisible ON")

    invisLoop = RunService.Heartbeat:Connect(function(dt)
        local limit = Workspace.FallenPartsDestroyHeight + 35
        if invisGhost and invisGhost.Parent == Workspace and gh and gh.Health > 0 and invisGhostRoot and invisGhostRoot.Position.Y > limit and h and h.Health > 0 then
            if cam and cam.CameraSubject ~= gh then cam.CameraSubject = gh end
            local currentState = gh:GetState()
            if currentState ~= Enum.HumanoidStateType.Climbing then
                invisLastSafeCFrame = invisGhostRoot.CFrame
                if UIS.MouseBehavior == Enum.MouseBehavior.LockCenter then
                    local look = cam.CFrame.LookVector
                    local targetRot = CFrame.lookAt(invisGhostRoot.Position, invisGhostRoot.Position + Vector3.new(look.X, 0, look.Z))
                    invisGhostRoot.CFrame = invisGhostRoot.CFrame:Lerp(targetRot, math.clamp(dt * 25, 0, 1))
                end
            end
            gh:Move(h.MoveDirection, false)
            gh.Jump = h.Jump
            r.CFrame = invisStaticPos
            local gv = invisGhostRoot.AssemblyLinearVelocity
            if gv.Magnitude > 120 then gv = gv.Unit * 120 end
            r.AssemblyLinearVelocity = gv
            r.AssemblyAngularVelocity = invisGhostRoot.AssemblyAngularVelocity
        else
            invisStop()
            state.invisibleEnabled = false
            updateAllToggleVisuals("invisibleEnabled")
        end
    end)
end

task.spawn(function()
    local last = false
    while true do
        task.wait(0.1)
        local now = state.invisibleEnabled == true
        if now ~= last then
            if now then invisStart() else invisStop() end
            last = now
        end
    end
end)

LP.CharacterRemoving:Connect(function()
    if invisActive then
        invisStop()
        state.invisibleEnabled = false
        updateAllToggleVisuals("invisibleEnabled")
    end
end)

-- ==================== ROLE NOTIFICATIONS ====================
local notifiedMurderer = false
local notifiedSheriff = false
local notifiedPerk = false
local murdererDetectedAt = 0

task.spawn(function()
    while true do
        task.wait(0.2)
        if not isInRound() then
            notifiedMurderer = false; notifiedSheriff = false; notifiedPerk = false; murdererDetectedAt = 0
        else
            if not notifiedMurderer then
                local mur = findPlayerByRole("Murderer")
                if mur then
                    notifiedMurderer = true; murdererDetectedAt = tick()
                    showNotification(mur.Name .. ' is the <font color="#ff4040">Murderer</font>')
                end
            end
            if not notifiedSheriff then
                local sher = findPlayerByRole("Sheriff") or findPlayerByRole("Hero")
                if sher then
                    notifiedSheriff = true
                    showNotification(sher.Name .. ' is the <font color="#4099ff">Sheriff</font>')
                end
            end
            if not notifiedPerk and notifiedMurderer then
                local mur = findPlayerByRole("Murderer")
                if mur then
                    local perk = getPlayerPerk(mur)
                    if perk then
                        notifiedPerk = true
                        showNotification(mur.Name .. ' is using <font color="#ff9d3b">' .. perk .. '</font>')
                    elseif murdererDetectedAt > 0 and (tick() - murdererDetectedAt) > 3 then
                        notifiedPerk = true
                    end
                end
            end
        end
    end
end)

-- ==================== ROUND TIMER ====================
task.spawn(function()
    while true do
        task.wait(0.1)
        if not state.showRoundTimerCountdown then
            if roundTimerFrame.Visible then roundTimerFrame.Visible = false end
        elseif isInRound() then
            local remaining = getRoundTimeRemaining()
            if remaining then
                local total = math.floor(remaining + 0.5)
                local mins = math.floor(total / 60)
                local secs = total % 60
                local newText = string.format("%d:%02d", mins, secs)
                if rtValue.Text ~= newText then rtValue.Text = newText end
                if not roundTimerFrame.Visible then roundTimerFrame.Visible = true end
                if total <= 30 then rtValue.TextColor3 = Color3.fromRGB(255, 80, 80); rtStroke.Color = Color3.fromRGB(255, 80, 80)
                elseif total <= 60 then rtValue.TextColor3 = Color3.fromRGB(255, 190, 80); rtStroke.Color = Color3.fromRGB(120, 100, 60)
                else rtValue.TextColor3 = Color3.fromRGB(255, 255, 255); rtStroke.Color = Color3.fromRGB(60, 60, 60) end
            else if roundTimerFrame.Visible then roundTimerFrame.Visible = false end end
        else if roundTimerFrame.Visible then roundTimerFrame.Visible = false end end
    end
end)

-- ==================== ANTI-VOID ====================
local lastAntiVoidFire = 0
task.spawn(function()
    while true do
        task.wait(0.15)
        if state.antiVoid and LP.Character and not invisActive then
            local root = LP.Character:FindFirstChild("HumanoidRootPart")
            if root and root.Position.Y < -400 then
                local now = tick()
                if now - lastAntiVoidFire > 4 then
                    lastAntiVoidFire = now
                    root.CFrame = LOBBY_CFRAME
                    root.AssemblyLinearVelocity = Vector3.zero
                    root.AssemblyAngularVelocity = Vector3.zero
                    setStatus("Anti-Void: sent to lobby")
                    showNotification('Tried to fall into the void? Why\'d you do that <font color="#ff4040">dumbass</font>')
                end
            end
        end
    end
end)

-- ==================== FARM LOOPS ====================
local function getCoins()
    local coins = {}
    for _, map in ipairs(Workspace:GetChildren()) do
        local cc = map:FindFirstChild("CoinContainer") or (map.Name == "CoinContainer" and map)
        if cc then
            for _, d in ipairs(cc:GetDescendants()) do
                if d:IsA("BasePart") and d:FindFirstChild("TouchInterest") then table.insert(coins, d) end
            end
        end
    end
    return coins
end

local function collectCoin(root, coin)
    if not root or not coin or not coin.Parent then return end
    pcall(function()
        for _ = 1, 3 do
            firetouchinterest(root, coin, 0); firetouchinterest(root, coin, 1)
        end
    end)
end

local function runBagFullAction()
    if busy then return end
    busy = true; isExecutingAction = true; bagFull = true
    local originalFarmState, originalXpState = state.farm, state.xpFarm
    state.farm, state.xpFarm = false, false
    pcall(function()
        cancelFarmTween(); totalCoinsEarned = totalCoinsEarned + currentCoinCount
        hideSky(); task.wait(0.3)
        local role = getRole()
        if role == "Murderer" then
            setStatus("Bag full! Pulling knife & killing all...")
            local knife = equipTool("Knife")
            if not knife then
                for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
                    if v:IsA("Tool") and v.Name == "Knife" then knife = v:Clone(); knife.Parent = LP.Character; break end
                end
            end
            autoKillAllPlayers()
            if state.autoResetMurderer then triggerMenuReset() task.wait(1.5) end
        else
            local mur = getMurd()
            if mur and mur.Character then
                setStatus("Bag full! Flinging Murderer as " .. role .. "...")
                task.spawn(function() reliableFling(mur) end)
            else setStatus("Bag full! Murderer not found to fling.") end
            hideSky(); task.wait(0.3)
            if role == "Innocent" and state.autoResetInnocent then triggerMenuReset() task.wait(1.5)
            elseif role == "Sheriff" and state.autoResetSheriff then triggerMenuReset() task.wait(1.5) end
        end
    end)
    bagFull = false; currentCoinCount = 0; hasCollectedThisRound = false
    isExecutingAction = false; busy = false; task.wait(0.5)
    state.farm, state.xpFarm = originalFarmState, originalXpState
end

task.spawn(function()
    local ok, remote = pcall(function()
        return ReplicatedStorage:WaitForChild("Remotes", 10):WaitForChild("Gameplay", 10):WaitForChild("CoinCollected", 10)
    end)
    if ok and remote then
        remote.OnClientEvent:Connect(function(_, currentCoins, maxCoins)
            if typeof(currentCoins) == "number" then
                currentCoinCount = math.clamp(currentCoins, 0, 50)
                if currentCoins > 0 then hasCollectedThisRound = true end
            end
            if typeof(maxCoins) == "number" then maxCoinCount = maxCoins end
            if typeof(currentCoins) == "number" and currentCoins <= 0 then currentCoinCount = 0; bagFull = false; return end
            if typeof(currentCoins) == "number" and currentCoins >= MIN_BAG_FULL and not isExecutingAction then task.spawn(runBagFullAction) end
        end)
    end
end)

task.spawn(function()
    while true do
        task.wait(0.2)
        local inRound = isInRound()
        local hasRoles = getMurd() ~= nil
        if (bagFull or isExecutingAction) and (not alive() or tick() - (lastCoinFoundTime or tick()) > 15) then
            bagFull = false; isExecutingAction = false; busy = false
            setStatus("Unstuck action...")
        end
        if not inRound then
            lastRoundState = false; roundFullyStarted = false; hasCollectedThisRound = false
            currentCoinCount = 0; bagFull = false; isExecutingAction = false; busy = false
        elseif not lastRoundState then
            lastRoundState = true; roundStartTime = tick(); setStatus("Map loaded...")
            if state.notifyMapName then
                local mapName = getMapName()
                if mapName then task.spawn(function() showNotification('Map loaded: <font color="#7ec8ff">' .. mapName .. '</font>') end) end
            end
        end
        if inRound and hasRoles and not roundFullyStarted then
            if (tick() - roundStartTime) > 6 then roundFullyStarted = true; setStatus("Round started") end
        end
    end
end)

task.spawn(function()
    while true do
        task.wait(0.04)
        if invisActive then cancelFarmTween() continue end
        if not alive() then cancelFarmTween() continue end
        local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        local humanoid = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if not root or not humanoid then continue end
        if state.xpFarm then
            cancelFarmTween(); humanoid.PlatformStand = true; root.Anchored = true
            root.CFrame = HIDE_POS; root.AssemblyLinearVelocity = Vector3.zero; root.AssemblyAngularVelocity = Vector3.zero
            continue
        end
        if not state.farm then cancelFarmTween() continue end
        if isLobby() or bagFull or isExecutingAction or next(activeResets) then
            cancelFarmTween()
            if not isExecutingAction and not next(activeResets) then
                humanoid.PlatformStand = true; root.Anchored = true; root.CFrame = HIDE_POS
                root.AssemblyLinearVelocity = Vector3.zero
            end
            lastCoinFoundTime = tick(); continue
        end
        root.Anchored = false; humanoid.PlatformStand = true; humanoid.AutoRotate = false
        root.AssemblyLinearVelocity = Vector3.zero; root.AssemblyAngularVelocity = Vector3.zero
        local coins = getCoins()
        if #coins > 0 then lastCoinFoundTime = tick()
        else
            if tick() - lastCoinFoundTime > NO_COIN_TIMEOUT then
                setStatus("No coins found timeout, resetting...")
                cancelFarmTween(); lastCoinFoundTime = tick()
                if not isLobby() then triggerMenuReset() task.wait(1.5) end
                continue
            end
        end
        if #coins == 0 then cancelFarmTween() continue end
        local closestCoin, shortest = nil, math.huge
        for _, c in ipairs(coins) do
            local dist = (root.Position - c.Position).Magnitude
            if dist < shortest then shortest = dist closestCoin = c end
        end
        if not closestCoin then cancelFarmTween() continue end
        local dist = (root.Position - closestCoin.Position).Magnitude
        if dist <= 7 then collectCoin(root, closestCoin) end
        if currentFarmTween and currentFarmTween.PlaybackState == Enum.PlaybackState.Playing and currentTargetCoin == closestCoin then
            collectCoin(root, closestCoin); continue
        end
        cancelFarmTween(); currentTargetCoin = closestCoin
        local targetPos = closestCoin.Position + Vector3.new(0, -UNDER, 0)
        local targetCFrame = CFrame.new(targetPos) * CFrame.Angles(math.rad(90), 0, math.rad(180))
        local duration = math.clamp(dist / TWEEN_SPEED, 0.05, 3.3)
        currentFarmTween = TweenService:Create(root, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = targetCFrame})
        currentFarmTween:Play()
        farmVelocityConn = RunService.Heartbeat:Connect(function()
            if not root or not root.Parent then cancelFarmTween() return end
            root.AssemblyLinearVelocity = Vector3.zero; root.AssemblyAngularVelocity = Vector3.zero
        end)
    end
end)

task.spawn(function()
    while true do
        task.wait(1)
        if state and state.autoPrestige then
            pcall(function()
                game:GetService("ReplicatedStorage").Remotes.Inventory.Prestige:FireServer()
                setStatus("Auto prestiged!")
            end)
        end
    end
end)

local queueOnTeleport = syn and syn.queue_on_teleport or queue_on_teleport or queueonteleport
local function setupAutoRejoinQueue()
    if not queueOnTeleport then return end
    pcall(function()
        queueOnTeleport([[
            task.wait(2)
            local ts = game:GetService("TeleportService")
            local p = game:GetService("Players").LocalPlayer
            ts:Teleport(game.PlaceId, p)
        ]])
    end)
end

task.spawn(function()
    while true do if state.autoRejoin then pcall(setupAutoRejoinQueue) end task.wait(10) end
end)

task.spawn(function()
    TeleportService.TeleportInitFailed:Connect(function(player)
        if player == LP and state.autoRejoin then task.wait(2) serverHop() end
    end)
    GuiService.ErrorMessageChanged:Connect(function()
        local errorText = GuiService:GetErrorMessage()
        if errorText and errorText ~= "" and state.autoRejoin then
            pcall(setupAutoRejoinQueue); TeleportService:Teleport(game.PlaceId, LP)
        end
    end)
    while true do
        task.wait(2)
        if state.autoRejoin then
            pcall(function()
                local promptGui = CoreGui:FindFirstChild("RobloxPromptGui")
                if promptGui then
                    local promptOverlay = promptGui:FindFirstChild("promptOverlay")
                    if promptOverlay then
                        for _, child in ipairs(promptOverlay:GetChildren()) do
                            if child.Name == "ErrorPrompt" then
                                pcall(setupAutoRejoinQueue); TeleportService:Teleport(game.PlaceId, LP)
                            end
                        end
                    end
                end
            end)
        end
    end
end)

local lastFarmNoclip = false
RunService.Stepped:Connect(function()
    if not LP.Character then lastFarmNoclip = false; return end
    if invisActive then return end
    local shouldNoclip = state.farm and not bagFull and not isExecutingAction and not isLobby() and not state.xpFarm and not next(activeResets)
    if shouldNoclip then
        for _, val in ipairs(LP.Character:GetDescendants()) do
            if val:IsA("BasePart") then val.CanCollide = false end
        end
        lastFarmNoclip = true
    elseif lastFarmNoclip then
        for _, val in ipairs(LP.Character:GetDescendants()) do
            if val:IsA("BasePart") then val.CanCollide = true end
        end
        lastFarmNoclip = false
    end
end)

task.spawn(function()
    while true do
        task.wait(3)
        if state.autoServerHop and #Players:GetPlayers() <= 2 and isLobby() then
            setStatus("Low player count in lobby, switching servers...")
            serverHop(); task.wait(10)
        end
    end
end)

task.spawn(function()
    while true do
        task.wait(0.15)
        if state.gun and alive() and not isExecutingAction and not findTool("Gun") and not invisActive then
            local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
            if root then
                for _, v in pairs(Workspace:GetDescendants()) do
                    if v.Name == "GunDrop" and v:IsA("BasePart") then
                        pcall(function() firetouchinterest(root, v, 0); firetouchinterest(root, v, 1) end)
                        break
                    end
                end
            end
        end
    end
end)

task.spawn(function()
    pcall(function()
        local vu = game:GetService("VirtualUser")
        LP.Idled:Connect(function()
            if state.afk then vu:CaptureController(); vu:ClickButton2(Vector2.new()) end
        end)
    end)
end)

local espFolder = Instance.new("Folder")
espFolder.Name = "AW_ESP_Container"
pcall(function() espFolder.Parent = getGuiParent() end)
local espCache = {}

local function clearESP(instance)
    if espCache[instance] then
        pcall(function()
            if espCache[instance].highlight then espCache[instance].highlight:Destroy() end
            if espCache[instance].billboard then espCache[instance].billboard:Destroy() end
        end)
        espCache[instance] = nil
    end
end

local function updateCreateESP(instance, textStr, color)
    if not instance or not instance.Parent then clearESP(instance) return end
    if not espCache[instance] then
        local highlight = Instance.new("Highlight")
        highlight.Adornee = instance; highlight.FillColor = color
        highlight.FillTransparency = 0.5; highlight.OutlineColor = color
        highlight.OutlineTransparency = 0.2
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        highlight.Parent = espFolder
        local billboard = Instance.new("BillboardGui")
        billboard.Adornee = instance
        billboard.Size = UDim2.new(0, 200, 0, 50)
        billboard.StudsOffset = Vector3.new(0, 4, 0)
        billboard.AlwaysOnTop = true; billboard.Parent = espFolder
        local text = Instance.new("TextLabel")
        text.Size = UDim2.new(1, 0, 1, 0); text.BackgroundTransparency = 1
        text.Text = textStr; text.TextColor3 = color; text.TextStrokeTransparency = 0
        text.Font = Enum.Font.GothamBold; text.TextSize = 12; text.Parent = billboard
        espCache[instance] = {highlight = highlight, billboard = billboard, text = text}
    else
        local esp = espCache[instance]
        if esp.highlight and esp.billboard and esp.text then
            esp.highlight.Adornee = instance; esp.billboard.Adornee = instance
            esp.highlight.FillColor = color; esp.highlight.OutlineColor = color
            esp.text.Text = textStr; esp.text.TextColor3 = color
        else clearESP(instance) end
    end
end

task.spawn(function()
    while true do
        task.wait(0.1)
        if state.espPlayers then
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LP and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
                    local role = getPlayerRole(player)
                    local color = Color3.fromRGB(0, 255, 0)
                    if role == "Murderer" then color = Color3.fromRGB(255, 0, 0)
                    elseif role == "Sheriff" then color = Color3.fromRGB(0, 150, 255)
                    elseif role == "Hero" then color = Color3.fromRGB(255, 255, 0) end
                    updateCreateESP(player.Character, string.format("%s [%s]", player.Name, role), color)
                else if player.Character then clearESP(player.Character) end end
            end
        else
            for _, player in ipairs(Players:GetPlayers()) do if player.Character then clearESP(player.Character) end end
        end
        local foundGunDrop = nil
        if isInRound() then
            for _, v in pairs(Workspace:GetDescendants()) do
                if v.Name == "GunDrop" and v:IsA("BasePart") then foundGunDrop = v break end
            end
        end
        if state.espGunDrop and foundGunDrop then
            updateCreateESP(foundGunDrop, "Gun Drop", Color3.fromRGB(255, 255, 0))
        else
            for inst in pairs(espCache) do
                if inst.Name == "GunDrop" then clearESP(inst) end
            end
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if not alive() then return end
    local targetPlayer = nil
    if state.aimbotMurderer then targetPlayer = getMurd()
    elseif state.aimbotSheriff then targetPlayer = getGunHolder() end
    if not targetPlayer or not targetPlayer.Character then return end
    local char = targetPlayer.Character
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum or hum.Health <= 0 then return end
    local targetPart = char:FindFirstChild("Head") or char:FindFirstChild("HumanoidRootPart")
    if not targetPart then return end
    Workspace.CurrentCamera.CFrame = CFrame.lookAt(Workspace.CurrentCamera.CFrame.Position, targetPart.Position)
end)

print("Appleware loaded - enjoy my script, love you though (made by p04j on discord)")
